---
phase: 01-platform-foundation-and-contracts
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/main.py
  - backend/app/api/routes/health.py
  - backend/app/api/routes/runs.py
  - backend/app/api/schemas/run.py
  - backend/tests/test_health.py
  - backend/tests/test_runs_contract.py
autonomous: true
requirements:
  - PLAT-01
must_haves:
  truths:
    - FastAPI service boots and serves a health endpoint
    - Analysis run lifecycle endpoint contracts are typed and validated
    - Backend test commands execute successfully for scaffolded endpoints
  artifacts:
    - path: backend/app/main.py
      provides: FastAPI app bootstrap and router registration
    - path: backend/app/api/schemas/run.py
      provides: Typed request and response contracts for analysis runs
    - path: backend/app/api/routes/runs.py
      provides: Run create/status endpoints
  key_links:
    - from: backend/app/main.py
      to: backend/app/api/routes/health.py
      via: router include
      pattern: include_router
    - from: backend/app/api/routes/runs.py
      to: backend/app/api/schemas/run.py
      via: typed request/response models
      pattern: BaseModel
---

<objective>
Stand up the backend foundation for phase 1 with typed FastAPI contracts for health and analysis run lifecycle.

Purpose: Establish PLAT-01-compliant backend API baseline that downstream ingestion and analytics plans can build on.
Output: Runnable FastAPI scaffold, typed schemas, and passing backend scaffold tests.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-platform-foundation-and-contracts/01-RESEARCH.md
@copilot-instructions.md

<interfaces>
Create and use these contracts in this plan:
- CreateRunRequest: repository_id (string), provider (github|gitlab), branch (optional string)
- CreateRunResponse: run_id (string), status (queued|running|succeeded|failed), created_at (ISO datetime)
- RunStatusResponse: run_id (string), status (queued|running|succeeded|failed), updated_at (ISO datetime), message (optional string)
- ErrorResponse: error_code (string), message (string), details (optional object)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Scaffold FastAPI app and health endpoint</name>
  <files>backend/app/main.py, backend/app/api/routes/health.py, backend/tests/test_health.py</files>
  <behavior>
    - GET /health returns HTTP 200 with service status payload
    - Backend app imports and boots without runtime errors
  </behavior>
  <action>Create backend app structure with FastAPI bootstrap in backend/app/main.py, add health router in backend/app/api/routes/health.py, and write tests in backend/tests/test_health.py before endpoint implementation. Ensure router is registered in main app.</action>
  <verify>
    <automated>cd backend; python -m pytest tests/test_health.py -q</automated>
  </verify>
  <done>Health endpoint exists, is routed from main app, and health test passes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement typed analysis run lifecycle contracts and routes</name>
  <files>backend/app/api/schemas/run.py, backend/app/api/routes/runs.py, backend/tests/test_runs_contract.py</files>
  <behavior>
    - POST /api/runs validates CreateRunRequest and returns CreateRunResponse
    - GET /api/runs/{run_id} returns typed RunStatusResponse
    - Invalid payloads return structured ErrorResponse
  </behavior>
  <action>Create Pydantic schemas in backend/app/api/schemas/run.py and implement route handlers in backend/app/api/routes/runs.py using those schemas. Add contract tests in backend/tests/test_runs_contract.py that validate required fields and status enum behavior. Keep orchestration stubbed; no forecasting logic in this phase.</action>
  <verify>
    <automated>cd backend; python -m pytest tests/test_runs_contract.py -q</automated>
  </verify>
  <done>Run endpoints compile, validate contracts, and contract tests pass.</done>
</task>

<task type="auto">
  <name>Task 3: Backend quality gate for phase scaffold</name>
  <files>backend/app/main.py, backend/app/api/routes/health.py, backend/app/api/routes/runs.py, backend/app/api/schemas/run.py</files>
  <action>Run backend quality gate and fix issues to keep strict typed baseline per PLAT-01. Keep implementation minimal but production-safe for startup checks and route imports.</action>
  <verify>
    <automated>cd backend; python -m pytest -q</automated>
  </verify>
  <done>All backend scaffold tests pass and API boot path is stable.</done>
</task>

</tasks>

<verification>
- Backend app starts via uvicorn using backend/app/main.py app object.
- /health and /api/runs endpoints respond with typed payload shapes.
- Backend pytest suite for scaffold passes.
</verification>

<success_criteria>
- FastAPI skeleton exists with typed analysis run lifecycle contracts (PLAT-01).
- Backend test baseline exists and passes for phase 1 scaffold behavior.
</success_criteria>

<output>
After completion, create `.planning/phases/01-platform-foundation-and-contracts/01-01-SUMMARY.md`
</output>
