---
phase: 01-platform-foundation-and-contracts
plan: 03
type: execute
wave: 2
depends_on:
  - 01-01
  - 01-02
files_modified:
  - contracts/v1/component.py
  - contracts/v1/health.py
  - contracts/v1/risk.py
  - contracts/v1/report.py
  - backend/app/domain/contracts/__init__.py
  - frontend/lib/contracts/index.ts
  - backend/tests/test_contract_alignment.py
autonomous: true
requirements:
  - PLAT-01
must_haves:
  truths:
    - Shared domain contracts for components, scores, risks, and reports are defined
    - Backend and frontend consume aligned contract definitions
    - Contract alignment checks run automatically
  artifacts:
    - path: contracts/v1/risk.py
      provides: canonical risk forecast contract
    - path: contracts/v1/report.py
      provides: canonical executive report summary contract
    - path: frontend/lib/contracts/index.ts
      provides: frontend-facing typed contract mirror
  key_links:
    - from: contracts/v1/*.py
      to: backend/app/domain/contracts/__init__.py
      via: backend contract exports
      pattern: import|export
    - from: contracts/v1/*.py
      to: frontend/lib/contracts/index.ts
      via: mirrored typed interfaces
      pattern: interface|type
---

<objective>
Define and wire shared domain contracts that keep backend and frontend payload semantics consistent across phase 1.

Purpose: Prevent contract drift early and satisfy phase requirement for typed request/response foundations.
Output: Canonical v1 domain contracts and backend/frontend alignment checks.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-platform-foundation-and-contracts/01-RESEARCH.md
@.planning/phases/01-platform-foundation-and-contracts/01-01-PLAN.md
@.planning/phases/01-platform-foundation-and-contracts/01-02-PLAN.md

<interfaces>
Canonical contracts to define (per phase 1 goal):
- ComponentProfile: component_id, repository_id, language, ownership, last_modified_at
- HealthScore: component_id, score (0-100), contributors (list), measured_at
- RiskForecast: component_id, horizon_days, risk_probability, confidence, top_signals
- ExecutiveReportSummary: report_id, run_id, executive_summary, cost_of_inaction_estimate, generated_at

Run lifecycle status enum remains: queued|running|succeeded|failed.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create canonical v1 domain contract definitions</name>
  <files>contracts/v1/component.py, contracts/v1/health.py, contracts/v1/risk.py, contracts/v1/report.py, backend/app/domain/contracts/__init__.py</files>
  <behavior>
    - Contract models exist for component, health, risk, and report artifacts
    - Contracts encode explicit field names and value semantics
  </behavior>
  <action>Create Python contract modules under contracts/v1 for each domain artifact and export backend contract entrypoints through backend/app/domain/contracts/__init__.py. Keep contracts strictly typed and add minimal metadata fields that support auditability (ids, timestamps, confidence/context fields).</action>
  <verify>
    <automated>cd backend; python -m pytest tests/test_contract_alignment.py -q</automated>
  </verify>
  <done>Canonical contract modules are defined and importable by backend.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Mirror contracts for frontend consumption</name>
  <files>frontend/lib/contracts/index.ts</files>
  <behavior>
    - Frontend has typed mirrors for all canonical domain contracts
    - Type definitions remain semantically aligned with backend contracts
  </behavior>
  <action>Create frontend/lib/contracts/index.ts with TypeScript interfaces/types mirroring canonical v1 contracts. Ensure field names and enum values match backend contracts exactly to avoid drift during later API wiring.</action>
  <verify>
    <automated>cd frontend; npm run typecheck</automated>
  </verify>
  <done>Frontend contract mirror compiles and matches canonical schema semantics.</done>
</task>

<task type="auto">
  <name>Task 3: Add contract alignment gate across backend/frontend</name>
  <files>backend/tests/test_contract_alignment.py, frontend/lib/contracts/index.ts</files>
  <action>Add alignment checks that fail when required contract fields or enums diverge between backend canonical contracts and frontend mirrors. Keep this gate lightweight but enforce field-level parity on core types.</action>
  <verify>
    <automated>cd backend; python -m pytest tests/test_contract_alignment.py -q ; cd ../frontend; npm run typecheck</automated>
  </verify>
  <done>Automated alignment gate exists and passes for all phase 1 contract artifacts.</done>
</task>

</tasks>

<verification>
- Canonical v1 contract files exist and are consumed by backend/frontend entrypoints.
- Contract alignment tests pass.
- Typed contract baseline is ready for ingestion and scoring phases.
</verification>

<success_criteria>
- Shared domain contracts for components, scores, risks, and reports are defined and aligned.
- Phase 1 contract foundation satisfies PLAT-01 and roadmap phase goal.
</success_criteria>

<output>
After completion, create `.planning/phases/01-platform-foundation-and-contracts/01-03-SUMMARY.md`
</output>
