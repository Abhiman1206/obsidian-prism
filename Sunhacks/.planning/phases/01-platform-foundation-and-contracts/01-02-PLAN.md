---
phase: 01-platform-foundation-and-contracts
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/app/layout.tsx
  - frontend/app/page.tsx
  - frontend/app/runs/page.tsx
  - frontend/components/layout/app-shell.tsx
  - frontend/styles/globals.css
  - frontend/tests/layout-shell.spec.ts
autonomous: false
requirements:
  - PLAT-01
must_haves:
  truths:
    - Next.js app shell loads and renders key placeholder routes
    - Layout is usable on both mobile and desktop breakpoints
    - Frontend build and type checks pass for scaffolded shell
  artifacts:
    - path: frontend/components/layout/app-shell.tsx
      provides: Responsive base shell wrapper
    - path: frontend/app/layout.tsx
      provides: App-wide layout wiring
    - path: frontend/tests/layout-shell.spec.ts
      provides: Layout shell test coverage
  key_links:
    - from: frontend/app/layout.tsx
      to: frontend/components/layout/app-shell.tsx
      via: root layout composition
      pattern: AppShell
    - from: frontend/app/page.tsx
      to: frontend/app/runs/page.tsx
      via: shared shell and route navigation
      pattern: Link|navigation
---

<objective>
Stand up a responsive Next.js application shell aligned to phase 1 contracts and roadmap routes.

Purpose: Provide a stable UI foundation for risk dashboards and report workflows that arrive in later phases.
Output: Next.js scaffolded shell, base routes, and frontend verification tests.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-platform-foundation-and-contracts/01-RESEARCH.md
@copilot-instructions.md

<interfaces>
UI placeholders in this plan should reserve contract fields for later wiring:
- KPI summary card placeholders: label, value, trend, confidence
- Run list row placeholders: run_id, status, created_at
- Report preview placeholders: report_id, executive_summary, generated_at
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create responsive app shell and route placeholders</name>
  <files>frontend/components/layout/app-shell.tsx, frontend/app/layout.tsx, frontend/app/page.tsx, frontend/app/runs/page.tsx, frontend/styles/globals.css</files>
  <behavior>
    - Root page and runs page render inside shared shell
    - Shell supports mobile-first layout and desktop expansion
  </behavior>
  <action>Implement responsive shell component and wire it into Next.js root layout. Create placeholder pages for home and runs routes with semantic sections for future KPI, risk, and report blocks. Ensure CSS establishes breakpoints and spacing tokens sufficient for mobile/desktop usability.</action>
  <verify>
    <automated>cd frontend; npm run typecheck ; npm run build</automated>
  </verify>
  <done>Shell is wired into root layout and routes compile/build successfully.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add shell-level verification tests</name>
  <files>frontend/tests/layout-shell.spec.ts</files>
  <behavior>
    - Home route renders shell container
    - Runs route renders shell container
    - Navigation links exist for primary scaffold routes
  </behavior>
  <action>Add automated UI test coverage for shell render and route placeholders. Use the project's chosen frontend test stack and keep tests deterministic for CI execution.</action>
  <verify>
    <automated>cd frontend; npm test -- layout-shell</automated>
  </verify>
  <done>Shell tests pass and detect regressions for base route composition.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify responsive shell behavior visually</name>
  <action>Perform human visual verification of responsive layout behavior after automated build/tests pass, then provide approval or defects for correction.</action>
  <what-built>Responsive Next.js shell and scaffold routes are implemented and tested.</what-built>
  <how-to-verify>
    1. Run frontend dev server and open the home route.
    2. Check layout at approximately 390px width: content remains readable and navigable.
    3. Check layout at approximately 1440px width: shell expands without broken spacing.
    4. Open runs route and confirm shared shell consistency.
  </how-to-verify>
  <verify>
    <automated>cd frontend; npm run typecheck ; npm run build ; npm test -- layout-shell</automated>
  </verify>
  <done>Human confirms responsive shell quality at target breakpoints or provides concrete issues to fix before approval.</done>
  <resume-signal>Type "approved" or describe issues to fix.</resume-signal>
</task>

</tasks>

<verification>
- Frontend typecheck and build pass.
- UI shell tests pass.
- Human verification confirms mobile/desktop usability baseline.
</verification>

<success_criteria>
- Next.js app shell exists with responsive layout foundation for phase 1.
- Base routes and shell-level tests are in place for downstream UI phases.
</success_criteria>

<output>
After completion, create `.planning/phases/01-platform-foundation-and-contracts/01-02-SUMMARY.md`
</output>
