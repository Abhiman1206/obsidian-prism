---
phase: 04-90-day-predictive-risk-forecasting
plan: 03
type: execute
wave: 3
depends_on: [04-02]
files_modified:
  - backend/tests/test_risk_forecasts_route_contract.py
  - backend/app/domain/risk/repository.py
  - backend/app/workers/risk_forecasting.py
  - backend/app/api/routes/risk_forecasts.py
  - backend/app/main.py
autonomous: true
requirements: [RISK-02, RISK-03]
must_haves:
  truths:
    - "Risk forecasts are retrievable per run as a ranked high-risk component list"
    - "Returned forecast rows include confidence and top contributing signals"
    - "Unknown run lookups return stable empty-list responses"
  artifacts:
    - path: "backend/app/domain/risk/repository.py"
      provides: "run-scoped risk forecast persistence and ranking retrieval"
    - path: "backend/app/api/routes/risk_forecasts.py"
      provides: "GET API for ranked risk forecasts by run"
    - path: "backend/tests/test_risk_forecasts_route_contract.py"
      provides: "route contract verification for ranked payload and empty run behavior"
  key_links:
    - from: "backend/app/workers/risk_forecasting.py"
      to: "backend/app/domain/risk/repository.py"
      via: "worker persistence helper writes forecast rows for route retrieval"
    - from: "backend/app/api/routes/risk_forecasts.py"
      to: "backend/app/domain/risk/repository.py"
      via: "route reads ranked rows by run_id"
---

<objective>
Expose ranked 90-day risk forecasts via API with run-scoped retrieval, confidence, and contributor details.

Purpose: Complete RISK-03 retrieval and finalize RISK-02 delivery through route-level contracts.
Output: Risk repository, persistence helper wiring, and ranked forecast API route.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-90-day-predictive-risk-forecasting/04-02-PLAN.md
@backend/app/domain/health/repository.py
@backend/app/api/routes/health_scores.py
@backend/app/main.py

<interfaces>
From backend/app/domain/health/repository.py:
- Repository pattern uses in-memory singleton with add_many and get_by_run methods.

From backend/app/api/routes/health_scores.py:
- Route pattern returns list[dict] and yields [] for unknown run id.

From 04-02 planned output:
- run_risk_forecasting(...) generates forecast rows containing risk_probability, confidence, and top_signals.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define failing route contract tests for ranked risk retrieval</name>
  <files>backend/tests/test_risk_forecasts_route_contract.py</files>
  <read_first>
    - backend/tests/test_health_scores_route_contract.py
    - backend/app/api/routes/health_scores.py
    - backend/app/main.py
    - .planning/phases/04-90-day-predictive-risk-forecasting/04-02-PLAN.md
  </read_first>
  <behavior>
    - GET /api/risk-forecasts/{run_id} returns a ranked list ordered by risk_probability desc then component_id asc.
    - Each row includes component_id, horizon_days, risk_probability, confidence, top_signals, run_id, repository_id, forecasted_at.
    - Unknown run returns HTTP 200 with [] payload.
  </behavior>
  <action>
    Add RED tests in backend/tests/test_risk_forecasts_route_contract.py:
    - Seed in-memory forecast data through worker persistence helper.
    - Assert route response ordering and required fields including top_signals contributor payload.
    - Assert unknown run returns empty list with status 200.
  </action>
  <verify>
    <automated>pytest backend/tests/test_risk_forecasts_route_contract.py -q</automated>
  </verify>
  <done>Failing tests define ranked retrieval contract and empty-run behavior for risk forecasts.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement risk repository, route, and application wiring</name>
  <files>backend/app/domain/risk/repository.py, backend/app/workers/risk_forecasting.py, backend/app/api/routes/risk_forecasts.py, backend/app/main.py</files>
  <read_first>
    - backend/tests/test_risk_forecasts_route_contract.py
    - backend/app/domain/health/repository.py
    - backend/app/api/routes/health_scores.py
  </read_first>
  <action>
    Implement run-scoped persistence and ranked retrieval:
    - Create backend/app/domain/risk/repository.py with singleton RiskForecastRepository supporting add_many(records) and get_ranked_by_run(run_id).
    - Update backend/app/workers/risk_forecasting.py with persist_risk_forecasts(records) helper writing to the repository.
    - Create backend/app/api/routes/risk_forecasts.py exposing GET /api/risk-forecasts/{run_id} returning get_ranked_by_run(run_id).
    - Register risk_forecasts router in backend/app/main.py.
    - Enforce deterministic ranking by risk_probability desc then component_id asc.
  </action>
  <verify>
    <automated>pytest backend/tests/test_risk_forecasts_route_contract.py -q</automated>
  </verify>
  <done>Ranked risk forecast retrieval route passes contract tests and exposes confidence plus contributor signals by run.</done>
</task>

</tasks>

<verification>
- pytest backend/tests/test_risk_forecasts_route_contract.py -q
</verification>

<success_criteria>
- RISK-03 ranked forecasts are retrievable per run through a stable API contract.
- RISK-02 confidence and top contributor signals are included in route payloads.
</success_criteria>

<output>
After completion, create .planning/phases/04-90-day-predictive-risk-forecasting/04-03-SUMMARY.md
</output>
