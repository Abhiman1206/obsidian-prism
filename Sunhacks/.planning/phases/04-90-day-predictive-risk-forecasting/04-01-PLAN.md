---
phase: 04-90-day-predictive-risk-forecasting
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/tests/test_risk_features.py
  - backend/app/domain/risk/__init__.py
  - backend/app/domain/risk/features.py
autonomous: true
requirements: [RISK-01]
must_haves:
  truths:
    - "Feature vectors are generated per component for 90-day risk forecasting inputs"
    - "Feature rows combine health score, churn intensity, and historical defect proxy signals"
    - "Feature extraction is deterministic for identical ingestion and health inputs"
  artifacts:
    - path: "backend/app/domain/risk/features.py"
      provides: "deterministic risk feature extraction from health and ingestion payloads"
    - path: "backend/tests/test_risk_features.py"
      provides: "TDD coverage for feature composition and deterministic outputs"
  key_links:
    - from: "backend/app/domain/risk/features.py"
      to: "backend/app/workers/health_scoring.py"
      via: "health score row inputs reused as risk feature baseline"
    - from: "backend/app/domain/risk/features.py"
      to: "backend/app/domain/ingestion/normalization.py"
      via: "canonical churn and commit summaries mapped into feature fields"
---

<objective>
Implement the risk feature pipeline that assembles health and ingestion-derived predictors for 90-day forecasting.

Purpose: Establish the deterministic feature foundation required by RISK-01 before model scoring.
Output: Risk feature extraction module and passing feature behavior tests.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-code-health-scoring-engine/03-03-SUMMARY.md
@backend/app/workers/health_scoring.py
@backend/app/domain/ingestion/normalization.py
@backend/app/domain/ingestion/provider_signals.py

<interfaces>
From backend/app/workers/health_scoring.py:
- run_health_scoring(run_id, repository_id, metric_rows, volatility_by_component) -> list[dict]
- scored row keys include component_id, score, contributors, factors, run_id, repository_id, measured_at

From backend/app/domain/ingestion/normalization.py:
- normalize_provider_payload(...) returns commits[] and churn[] canonical structures
- churn row keys include contributor, commits_last_30d, files_touched_last_30d

From backend/app/domain/ingestion/provider_signals.py:
- extract_cadence_signals(...) returns issue_opened_count, issue_closed_count, deployment_count, period_start, period_end
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define failing tests for deterministic risk feature extraction</name>
  <files>backend/tests/test_risk_features.py</files>
  <read_first>
    - backend/tests/test_health_scoring.py
    - backend/tests/test_provider_normalization.py
    - backend/tests/test_provider_signals.py
    - backend/app/workers/health_scoring.py
  </read_first>
  <behavior>
    - Risk feature rows are keyed by component_id and include horizon_days=90.
    - Feature rows include health_score, contributor_churn_intensity, deployment_cadence, and defect_signal_proxy.
    - Same input payloads produce identical output order and values.
  </behavior>
  <action>
    Create RED tests in backend/tests/test_risk_features.py that define RISK-01 feature behavior:
    - Build fixture inputs from health score rows plus canonical ingestion cadence/churn payloads.
    - Assert each output row includes component_id and horizon_days=90.
    - Assert required feature keys exist: health_score, contributor_churn_intensity, deployment_cadence, defect_signal_proxy.
    - Assert deterministic ordering by descending feature_risk_pressure and stable floating values to 4 decimals.
  </action>
  <verify>
    <automated>pytest backend/tests/test_risk_features.py -q</automated>
  </verify>
  <done>Failing tests fully specify deterministic feature engineering behavior for RISK-01.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement risk feature extraction module and exports</name>
  <files>backend/app/domain/risk/features.py, backend/app/domain/risk/__init__.py</files>
  <read_first>
    - backend/tests/test_risk_features.py
    - backend/app/domain/ingestion/normalization.py
    - backend/app/domain/ingestion/provider_signals.py
    - backend/app/workers/health_scoring.py
  </read_first>
  <action>
    Implement deterministic feature engineering in backend/app/domain/risk/features.py:
    - Add a pure function build_risk_features(health_rows: list[dict], ingestion_payload: dict, horizon_days: int = 90) -> list[dict].
    - Compute contributor_churn_intensity from normalized churn counts.
    - Compute deployment_cadence from cadence deployment_count.
    - Compute defect_signal_proxy from issue_opened_count minus issue_closed_count, clamped at zero.
    - Derive feature_risk_pressure as deterministic weighted sum for sorting only.
    - Return sorted rows by feature_risk_pressure desc then component_id asc for stable order.
    - Export build_risk_features from backend/app/domain/risk/__init__.py.
  </action>
  <verify>
    <automated>pytest backend/tests/test_risk_features.py -q</automated>
  </verify>
  <done>Risk feature extraction module produces deterministic, test-verified 90-day feature rows.</done>
</task>

</tasks>

<verification>
- pytest backend/tests/test_risk_features.py -q
</verification>

<success_criteria>
- RISK-01 feature vectors are available per component with deterministic composition.
- Feature outputs integrate health and ingestion signals needed by forecasting.
</success_criteria>

<output>
After completion, create .planning/phases/04-90-day-predictive-risk-forecasting/04-01-SUMMARY.md
</output>
