---
phase: 06-responsive-next-js-dashboard-and-reporting-ux
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/app/reports/page.tsx
  - frontend/lib/api/reports.ts
  - frontend/lib/contracts/index.ts
  - frontend/components/reports/report-summary-card.tsx
  - frontend/components/reports/report-evidence-panel.tsx
  - frontend/tests/reports-page.spec.ts
autonomous: true
requirements:
  - UI-02
must_haves:
  truths:
    - "User can open executive reports and read sectioned business summaries."
    - "User can expand evidence claims and see lineage references for each claim."
    - "Report page supports deterministic selection of a report from a run scope."
  artifacts:
    - path: "frontend/app/reports/page.tsx"
      provides: "Executive report page with drill-down interactions"
      min_lines: 50
    - path: "frontend/lib/api/reports.ts"
      provides: "Executive report retrieval and parsing helpers"
      exports: ["getExecutiveReports"]
    - path: "frontend/components/reports/report-evidence-panel.tsx"
      provides: "Claim and lineage drill-down UI"
      exports: ["ReportEvidencePanel"]
  key_links:
    - from: "frontend/app/reports/page.tsx"
      to: "frontend/lib/api/reports.ts"
      via: "data loader call"
      pattern: "getExecutiveReports"
    - from: "frontend/lib/api/reports.ts"
      to: "/api/executive-reports/{run_id}"
      via: "fetch"
      pattern: "executive-reports"
---

<objective>
Deliver executive report UI with section-level evidence drill-down interactions.

Purpose: Satisfy UI-02 by allowing leadership users to inspect report claims and supporting lineage evidence.
Output: Reports page with claim panel and lineage link display.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-business-impact-synthesis/05-02-SUMMARY.md
@.planning/phases/05-business-impact-synthesis/05-03-SUMMARY.md
@frontend/lib/contracts/index.ts
@backend/app/workers/business_reporting.py
@backend/app/api/routes/executive_reports.py

<interfaces>
From backend/app/workers/business_reporting.py:
```python
report["claims"] = [
  {
    "claim_id": str,
    "claim_text": str,
    "lineage_refs": [str, ...],
  }
]
```

From backend/app/api/routes/executive_reports.py:
```python
@router.get("/{run_id}")
def get_executive_reports(run_id: str) -> list[dict]:
    return EXECUTIVE_REPORT_REPOSITORY.get_by_run(run_id)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add reports-page behavior tests for section rendering and evidence drill-down</name>
  <files>frontend/tests/reports-page.spec.ts</files>
  <behavior>
    - Test 1: Page renders executive summary, top risks, and recommended priorities.
    - Test 2: Claim panel shows claim_text and lineage_refs when expanded.
    - Test 3: Unknown or empty payload renders non-blocking empty state.
  </behavior>
  <action>Create report page tests (per UI-02) with mocked API payloads containing claims and lineage_refs; validate interactive reveal behavior without network calls.</action>
  <verify>
    <automated>cd frontend; npm run test -- tests/reports-page.spec.ts</automated>
  </verify>
  <done>Tests fail against current codebase and capture required executive report UX behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement report retrieval client, contract extensions, and evidence drill-down UI</name>
  <files>frontend/lib/contracts/index.ts, frontend/lib/api/reports.ts, frontend/components/reports/report-summary-card.tsx, frontend/components/reports/report-evidence-panel.tsx, frontend/app/reports/page.tsx</files>
  <behavior>
    - Report contracts include claim objects with lineage references.
    - Reports page can select a report for run_id and show section details.
    - Evidence panel supports expand/collapse interaction for claims.
  </behavior>
  <action>Extend frontend report contracts with claim/evidence structures compatible with backend payloads, implement getExecutiveReports client helper, create summary/evidence components, and build frontend/app/reports/page.tsx with run-scoped fetch and section-level drill-down (per UI-02).</action>
  <verify>
    <automated>cd frontend; npm run test -- tests/reports-page.spec.ts; npm run typecheck; npm run build</automated>
  </verify>
  <done>Executive report page loads, renders business sections, and exposes evidence drill-down with passing tests/typecheck/build.</done>
</task>

</tasks>

<verification>
- cd frontend; npm run test -- tests/reports-page.spec.ts
- cd frontend; npm run typecheck
- cd frontend; npm run build
</verification>

<success_criteria>
1. /reports displays executive report sections from backend payloads.
2. Users can inspect claim-level lineage references without leaving the page.
3. Empty-run behavior is informative and non-breaking.
</success_criteria>

<output>
After completion, create `.planning/phases/06-responsive-next-js-dashboard-and-reporting-ux/06-02-SUMMARY.md`
</output>
