---
phase: 06-responsive-next-js-dashboard-and-reporting-ux
plan: 03
type: execute
wave: 2
depends_on:
  - 06-01
  - 06-02
files_modified:
  - frontend/components/layout/app-shell.tsx
  - frontend/styles/globals.css
  - frontend/tests/layout-shell.spec.ts
  - frontend/tests/dashboard-page.spec.ts
  - frontend/tests/reports-page.spec.ts
autonomous: false
requirements:
  - UI-03
must_haves:
  truths:
    - "Dashboard and reports pages remain usable on mobile and desktop breakpoints."
    - "Primary navigation exposes direct access to dashboard and reports workflows."
    - "Critical interactive elements have visible focus and accessible labels."
  artifacts:
    - path: "frontend/components/layout/app-shell.tsx"
      provides: "Primary nav links for dashboard and reports"
      min_lines: 25
    - path: "frontend/styles/globals.css"
      provides: "Responsive and accessibility polish styles"
      min_lines: 90
    - path: "frontend/tests/layout-shell.spec.ts"
      provides: "Navigation/accessibility regression checks"
      min_lines: 30
  key_links:
    - from: "frontend/components/layout/app-shell.tsx"
      to: "frontend/app/page.tsx"
      via: "Dashboard nav link"
      pattern: "href=\"/\""
    - from: "frontend/components/layout/app-shell.tsx"
      to: "frontend/app/reports/page.tsx"
      via: "Reports nav link"
      pattern: "href=\"/reports\""
---

<objective>
Polish responsive behavior and accessibility for critical dashboard/report user journeys.

Purpose: Satisfy UI-03 by hardening cross-breakpoint usability and interaction clarity.
Output: Finalized nav/responsive/accessibility polish with regression coverage and human verification.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-responsive-next-js-dashboard-and-reporting-ux/06-01-PLAN.md
@.planning/phases/06-responsive-next-js-dashboard-and-reporting-ux/06-02-PLAN.md
@frontend/components/layout/app-shell.tsx
@frontend/styles/globals.css
@frontend/tests/layout-shell.spec.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add responsive and accessibility regression checks for shell, dashboard, and reports</name>
  <files>frontend/tests/layout-shell.spec.ts, frontend/tests/dashboard-page.spec.ts, frontend/tests/reports-page.spec.ts</files>
  <behavior>
    - Nav includes links for Home, Runs, and Reports.
    - Core headings and landmark regions remain discoverable for screen-reader queries.
    - Key interactive controls maintain deterministic accessible names.
  </behavior>
  <action>Expand existing layout-shell and page tests to assert navigation reachability, semantic heading/landmark presence, and accessibility labels for drill-down controls (per UI-03).</action>
  <verify>
    <automated>cd frontend; npm run test -- tests/layout-shell.spec.ts tests/dashboard-page.spec.ts tests/reports-page.spec.ts</automated>
  </verify>
  <done>Regression suite fails first where polish is missing and captures cross-screen expectations.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement cross-breakpoint and focus-state polish in shell and global styles</name>
  <files>frontend/components/layout/app-shell.tsx, frontend/styles/globals.css</files>
  <behavior>
    - Shell navigation remains clear on narrow screens.
    - Focus states are visible for keyboard users.
    - Dashboard/report container spacing and typography remain readable across breakpoints.
  </behavior>
  <action>Update app shell navigation to include reports entry and robust responsive wrapping; refine globals.css with breakpoint-specific layout spacing, card/table overflow handling, and explicit focus-visible styles for links/buttons.</action>
  <verify>
    <automated>cd frontend; npm run test -- tests/layout-shell.spec.ts tests/dashboard-page.spec.ts tests/reports-page.spec.ts; npm run typecheck; npm run build</automated>
  </verify>
  <done>All UI regression tests pass and responsive/focus polish is applied across shell/dashboard/report pages.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verification of mobile and desktop UX flows</name>
  <action>Run a final human QA pass on mobile and desktop breakpoints to validate responsive readability, keyboard focus visibility, and top-level navigation flow completion.</action>
  <verify>
    <automated>cd frontend; npm run test -- tests/layout-shell.spec.ts tests/dashboard-page.spec.ts tests/reports-page.spec.ts</automated>
  </verify>
  <done>Human confirmation received that dashboard/report flows are usable on mobile and desktop, or actionable defect notes captured for follow-up.</done>
  <what-built>Responsive dashboard and report workflows with accessibility-focused shell/nav polish.</what-built>
  <how-to-verify>
    1. Run `cd frontend; npm run dev`.
    2. Open `/` and `/reports` at desktop width (~1280px) and mobile width (~390px).
    3. Confirm ranked risk content and executive report sections are readable without horizontal clipping.
    4. Keyboard-tab through nav and report drill-down controls; confirm visible focus indicators.
    5. Confirm nav provides direct links to Home, Runs, and Reports.
  </how-to-verify>
  <resume-signal>Type "approved" to continue, or provide exact UI defects to fix.</resume-signal>
</task>

</tasks>

<verification>
- cd frontend; npm run test -- tests/layout-shell.spec.ts tests/dashboard-page.spec.ts tests/reports-page.spec.ts
- cd frontend; npm run typecheck
- cd frontend; npm run build
</verification>

<success_criteria>
1. Dashboard and report screens are usable and legible at mobile and desktop widths.
2. Keyboard focus states are visible and interactive controls are labeled.
3. Primary workflows (risk scan and report drill-down) are reachable from top-level navigation.
</success_criteria>

<output>
After completion, create `.planning/phases/06-responsive-next-js-dashboard-and-reporting-ux/06-03-SUMMARY.md`
</output>
