---
phase: 06-responsive-next-js-dashboard-and-reporting-ux
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/app/page.tsx
  - frontend/lib/api/risk.ts
  - frontend/components/dashboard/risk-kpi-grid.tsx
  - frontend/components/dashboard/risk-table.tsx
  - frontend/tests/dashboard-page.spec.ts
autonomous: true
requirements:
  - UI-01
must_haves:
  truths:
    - "User can view ranked risk components on the dashboard."
    - "User can see KPI summary values derived from risk data."
    - "Dashboard uses live API data when available and shows deterministic fallbacks when empty."
  artifacts:
    - path: "frontend/app/page.tsx"
      provides: "Dashboard page rendering wired to risk data client"
      min_lines: 40
    - path: "frontend/lib/api/risk.ts"
      provides: "Risk forecast fetch and transformation helpers"
      exports: ["getRiskForecasts", "buildRiskDashboardSummary"]
    - path: "frontend/components/dashboard/risk-table.tsx"
      provides: "Ranked component table"
      exports: ["RiskTable"]
  key_links:
    - from: "frontend/app/page.tsx"
      to: "frontend/lib/api/risk.ts"
      via: "data loader call"
      pattern: "getRiskForecasts"
    - from: "frontend/lib/api/risk.ts"
      to: "/api/risk-forecasts/{run_id}"
      via: "fetch"
      pattern: "risk-forecasts"
---

<objective>
Deliver a responsive risk dashboard page with ranked components and KPI summaries.

Purpose: Satisfy UI-01 by making risk signals visible and scannable for leadership users.
Output: Dashboard page wired to backend risk forecasts with deterministic ranking and KPI cards.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-platform-foundation-and-contracts/01-02-SUMMARY.md
@.planning/phases/04-90-day-predictive-risk-forecasting/04-03-SUMMARY.md
@frontend/app/page.tsx
@frontend/lib/contracts/index.ts
@backend/app/api/routes/risk_forecasts.py

<interfaces>
From frontend/lib/contracts/index.ts:
```typescript
export interface RiskForecast {
  component_id: string;
  horizon_days: number;
  risk_probability: number;
  confidence: number;
  top_signals: RiskSignal[];
}

export interface RiskSignal {
  signal_name: string;
  contribution_strength: number;
}
```

From backend/app/api/routes/risk_forecasts.py:
```python
@router.get("/{run_id}")
def get_risk_forecasts(run_id: str) -> list[dict]:
    return RISK_FORECAST_REPOSITORY.get_ranked_by_run(run_id)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add dashboard behavior tests for KPI and ranking output</name>
  <files>frontend/tests/dashboard-page.spec.ts</files>
  <behavior>
    - Test 1: Dashboard renders ranked component rows in descending risk order.
    - Test 2: KPI cards render total components, high-risk count, and average confidence.
    - Test 3: Empty API payload renders explicit empty-state copy.
  </behavior>
  <action>Create focused tests for dashboard behaviors (per UI-01) using mocked fetch responses and deterministic fixtures; keep tests isolated from network by stubbing global fetch.</action>
  <verify>
    <automated>cd frontend; npm run test -- tests/dashboard-page.spec.ts</automated>
  </verify>
  <done>Tests fail first against current placeholders and fully describe expected dashboard behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement risk data client and responsive dashboard composition</name>
  <files>frontend/lib/api/risk.ts, frontend/components/dashboard/risk-kpi-grid.tsx, frontend/components/dashboard/risk-table.tsx, frontend/app/page.tsx</files>
  <behavior>
    - Dashboard consumes transformed API records and preserves deterministic ranking from backend.
    - KPI cards compute totals from fetched data without hardcoded values.
    - Page remains usable on narrow and wide breakpoints.
  </behavior>
  <action>Implement getRiskForecasts and buildRiskDashboardSummary in frontend/lib/api/risk.ts, add KPI/table presentational components, and replace placeholder dashboard in frontend/app/page.tsx with the composed UI; preserve existing AppShell pattern and use run_id query parameter default fallback "latest" for deterministic loading.</action>
  <verify>
    <automated>cd frontend; npm run test -- tests/dashboard-page.spec.ts; npm run typecheck; npm run build</automated>
  </verify>
  <done>Dashboard displays real ranked component rows and KPI summaries with passing tests/typecheck/build.</done>
</task>

</tasks>

<verification>
Run frontend dashboard-focused tests and full frontend type/build gates:
- cd frontend; npm run test -- tests/dashboard-page.spec.ts
- cd frontend; npm run typecheck
- cd frontend; npm run build
</verification>

<success_criteria>
1. Homepage renders ranked risk components from /api/risk-forecasts/{run_id}.
2. KPI cards clearly summarize risk posture (component count, high-risk count, confidence).
3. Dashboard remains readable on mobile and desktop widths.
</success_criteria>

<output>
After completion, create `.planning/phases/06-responsive-next-js-dashboard-and-reporting-ux/06-01-SUMMARY.md`
</output>
