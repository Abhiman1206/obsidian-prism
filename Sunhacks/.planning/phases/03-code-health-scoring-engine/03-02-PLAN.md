---
phase: 03-code-health-scoring-engine
plan: 02
type: execute
wave: 2
depends_on: [03-01]
files_modified:
  - backend/tests/test_health_scoring.py
  - backend/app/domain/health/scoring.py
  - backend/app/workers/health_scoring.py
autonomous: true
requirements: [HEALTH-02]
must_haves:
  truths:
    - "Each component receives a normalized health score in range [0,100]"
    - "Score composition is stable and explainable via weighted factors"
  artifacts:
    - path: "backend/app/domain/health/scoring.py"
      provides: "deterministic score normalization and weighted aggregation"
    - path: "backend/app/workers/health_scoring.py"
      provides: "orchestration from metrics input to scored output"
  key_links:
    - from: "backend/app/domain/health/scoring.py"
      to: "backend/tests/test_health_scoring.py"
      via: "fixed-weight and clamping assertions"
    - from: "backend/app/workers/health_scoring.py"
      to: "backend/app/domain/health/scoring.py"
      via: "worker delegates aggregation to pure scoring function"
---

<objective>
Implement deterministic health score normalization and weighted aggregation from metric and volatility inputs.

Purpose: Satisfy HEALTH-02 with stable, test-anchored scoring behavior.
Output: Scoring module + worker wrapper + passing score tests.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-code-health-scoring-engine/03-RESEARCH.md
@backend/app/domain/ingestion/normalization.py
@backend/app/domain/ingestion/provider_signals.py
@backend/app/workers/provider_ingestion.py
@contracts/v1/health.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define scoring tests for fixed weights and clamped normalization</name>
  <files>backend/tests/test_health_scoring.py</files>
  <read_first>
    - backend/tests/test_provider_signals.py
    - backend/tests/test_provider_normalization.py
    - contracts/v1/health.py
  </read_first>
  <action>
    Create `backend/tests/test_health_scoring.py` first (RED) with tests that define deterministic HEALTH-02 behavior:
    - A component with metric inputs receives score in `[0,100]`.
    - Weight constants are exactly:
      - maintainability: 0.45
      - complexity: 0.35
      - volatility: 0.20
    - Normalization clamps all sub-scores to `[0,1]`.
    - Returned factors list includes exactly three names: `maintainability`, `complexity`, `volatility`.
    - Lower complexity and volatility with higher maintainability produce higher final score than inverse case.
  </action>
  <acceptance_criteria>
    - `backend/tests/test_health_scoring.py` contains `0.45`, `0.35`, and `0.20`
    - `backend/tests/test_health_scoring.py` contains assertion for score range `[0, 100]`
    - `backend/tests/test_health_scoring.py` contains factor-name assertions for maintainability/complexity/volatility
  </acceptance_criteria>
  <verify>
    <automated>pytest backend/tests/test_health_scoring.py -q</automated>
  </verify>
  <done>Failing tests define deterministic normalization and weighting behavior for HEALTH-02.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement scoring core and health scoring worker</name>
  <files>backend/app/domain/health/scoring.py, backend/app/workers/health_scoring.py</files>
  <read_first>
    - backend/tests/test_health_scoring.py
    - backend/app/workers/provider_ingestion.py
    - contracts/v1/health.py
  </read_first>
  <action>
    Implement deterministic scoring logic and worker orchestration.

    In `backend/app/domain/health/scoring.py`:
    - Define constants:
      - `MAINTAINABILITY_WEIGHT = 0.45`
      - `COMPLEXITY_WEIGHT = 0.35`
      - `VOLATILITY_WEIGHT = 0.20`
    - Implement helpers:
      - `clamp01(value: float) -> float`
      - normalization functions for maintainability, complexity, volatility
    - Implement `score_component(...) -> dict` returning:
      - `component_id`, `score`, `contributors`, `factors`
      - factors include `name`, `weight`, `raw_value`, `normalized_value`, `direction`

    In `backend/app/workers/health_scoring.py`:
    - Implement `run_health_scoring(run_id: str, repository_id: str, metric_rows: list[dict], volatility_by_component: dict[str, float]) -> list[dict]`
    - For each metric row, merge volatility input and call `score_component`.
    - Attach `run_id`, `repository_id`, and `measured_at` ISO timestamp to each scored output.
  </action>
  <acceptance_criteria>
    - `backend/app/domain/health/scoring.py` contains `MAINTAINABILITY_WEIGHT = 0.45`
    - `backend/app/domain/health/scoring.py` contains `def score_component(`
    - `backend/app/domain/health/scoring.py` contains `def clamp01(`
    - `backend/app/workers/health_scoring.py` contains `def run_health_scoring(`
    - `backend/app/workers/health_scoring.py` adds `run_id` and `repository_id` to each result
  </acceptance_criteria>
  <verify>
    <automated>pytest backend/tests/test_health_scoring.py -q</automated>
  </verify>
  <done>Scoring implementation passes deterministic weighting and explainability tests.</done>
</task>

</tasks>

<verification>
- `pytest backend/tests/test_health_scoring.py -q`
</verification>

<success_criteria>
- HEALTH-02 scoring is deterministic, normalized, and test-verified.
- Health scoring worker composes score payloads with run/repository metadata.
</success_criteria>

<output>
After completion, create `.planning/phases/03-code-health-scoring-engine/03-02-SUMMARY.md`
</output>
