---
phase: 03-code-health-scoring-engine
plan: 03
type: execute
wave: 3
depends_on: [03-02]
files_modified:
  - backend/app/domain/health/repository.py
  - backend/app/api/routes/health_scores.py
  - backend/app/workers/health_scoring.py
  - backend/app/main.py
  - backend/tests/test_health_scores_route_contract.py
autonomous: true
requirements: [HEALTH-03]
must_haves:
  truths:
    - "Health score results are persisted per run and queryable via API"
    - "API output includes explainability factors for each score"
  artifacts:
    - path: "backend/app/domain/health/repository.py"
      provides: "run-scoped storage/query boundary for health scores"
    - path: "backend/app/api/routes/health_scores.py"
      provides: "GET route exposing health scores by run"
  key_links:
    - from: "backend/app/api/routes/health_scores.py"
      to: "backend/app/domain/health/repository.py"
      via: "route reads from repository singleton boundary"
    - from: "backend/app/main.py"
      to: "health scores router"
      via: "FastAPI route registration"
---

<objective>
Persist scored health outputs and expose explainability metadata through a typed run-scoped API.

Purpose: Complete HEALTH-03 by making contributors/factors retrievable for downstream risk and business phases.
Output: Health score repository + route + route tests + app wiring.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@backend/app/api/routes/lineage.py
@backend/app/domain/evidence/repository.py
@backend/app/workers/lineage_ingestion.py
@backend/app/main.py
@backend/app/workers/health_scoring.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define route contract tests for health score explainability payload</name>
  <files>backend/tests/test_health_scores_route_contract.py</files>
  <read_first>
    - backend/tests/test_lineage_route_contract.py
    - contracts/v1/health.py
    - backend/app/api/routes/runs.py
  </read_first>
  <action>
    Create `backend/tests/test_health_scores_route_contract.py` first (RED) to define API behavior:
    - `GET /api/health-scores/{run_id}` returns 200 with list payload.
    - Each item includes: `component_id`, `score`, `run_id`, `repository_id`, `contributors`, `factors`, `measured_at`.
    - At least one factor item includes: `name`, `weight`, `raw_value`, `normalized_value`, `direction`.
    - Querying unknown run returns empty list (200) for stable client behavior.
  </action>
  <acceptance_criteria>
    - `backend/tests/test_health_scores_route_contract.py` contains route path `/api/health-scores/`
    - test file asserts presence of `factors` and `direction`
    - test file asserts empty-list behavior for unknown run
  </acceptance_criteria>
  <verify>
    <automated>pytest backend/tests/test_health_scores_route_contract.py -q</automated>
  </verify>
  <done>Route contract tests define required HEALTH-03 explainability API behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement health score repository and route wiring</name>
  <files>backend/app/domain/health/repository.py, backend/app/api/routes/health_scores.py, backend/app/workers/health_scoring.py, backend/app/main.py</files>
  <read_first>
    - backend/tests/test_health_scores_route_contract.py
    - backend/app/domain/evidence/repository.py
    - backend/app/api/routes/lineage.py
    - backend/app/workers/health_scoring.py
    - backend/app/main.py
  </read_first>
  <action>
    Implement persistence and read API boundary for scored health outputs.

    Create `backend/app/domain/health/repository.py`:
    - `HealthScoreRepository` with in-memory store list.
    - Methods:
      - `add_many(records: list[dict]) -> None`
      - `get_by_run(run_id: str) -> list[dict]`

    Update `backend/app/workers/health_scoring.py`:
    - Add module-level `HEALTH_SCORE_REPOSITORY = HealthScoreRepository()`
    - Add helper `persist_health_scores(records: list[dict]) -> None` that calls `add_many`.

    Create `backend/app/api/routes/health_scores.py`:
    - Router prefix `/api/health-scores`
    - `GET /{run_id}` endpoint returning repository `get_by_run(run_id)`.

    Update `backend/app/main.py`:
    - Register health score router via `app.include_router(...)`.
  </action>
  <acceptance_criteria>
    - `backend/app/domain/health/repository.py` contains `class HealthScoreRepository:`
    - `backend/app/domain/health/repository.py` contains `def get_by_run(`
    - `backend/app/workers/health_scoring.py` contains `HEALTH_SCORE_REPOSITORY`
    - `backend/app/api/routes/health_scores.py` contains prefix `/api/health-scores`
    - `backend/app/main.py` includes health score router registration
  </acceptance_criteria>
  <verify>
    <automated>pytest backend/tests/test_health_scores_route_contract.py -q</automated>
  </verify>
  <done>Health scores persist and are retrievable by run with explainability factors in response payload.</done>
</task>

</tasks>

<verification>
- `pytest backend/tests/test_health_scores_route_contract.py -q`
- `pytest backend/tests/test_health_metrics.py backend/tests/test_health_scoring.py backend/tests/test_health_scores_route_contract.py -q`
</verification>

<success_criteria>
- HEALTH-03 API exists and returns explainability-rich health score payloads.
- Route is wired in FastAPI app and stable for empty-run queries.
</success_criteria>

<output>
After completion, create `.planning/phases/03-code-health-scoring-engine/03-03-SUMMARY.md`
</output>
