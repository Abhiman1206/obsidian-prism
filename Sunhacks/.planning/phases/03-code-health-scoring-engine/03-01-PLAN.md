---
phase: 03-code-health-scoring-engine
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - contracts/v1/health.py
  - frontend/lib/contracts/index.ts
  - backend/tests/test_contract_alignment.py
  - backend/tests/test_health_metrics.py
  - backend/app/domain/health/metrics.py
autonomous: true
requirements: [HEALTH-01, HEALTH-03]
must_haves:
  truths:
    - "System computes Radon-style complexity and maintainability metrics per component/file"
    - "Health score contract can carry explainability factor metadata"
  artifacts:
    - path: "backend/app/domain/health/metrics.py"
      provides: "Radon metrics extraction with deterministic fallback"
    - path: "contracts/v1/health.py"
      provides: "Expanded HealthScore contract with factor metadata"
  key_links:
    - from: "contracts/v1/health.py"
      to: "frontend/lib/contracts/index.ts"
      via: "mirrored fields checked by contract alignment tests"
    - from: "backend/app/domain/health/metrics.py"
      to: "backend/tests/test_health_metrics.py"
      via: "TDD assertions for computed metrics and fallback"
---

<objective>
Establish deterministic source-code metric extraction and update health contracts to carry explainability factors.

Purpose: Deliver HEALTH-01 metric foundation and HEALTH-03 explainability contract surface before scoring logic.
Output: Metric extraction module + expanded backend/frontend health contracts + passing tests.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-code-health-scoring-engine/03-RESEARCH.md
@backend/app/domain/contracts/__init__.py
@contracts/v1/health.py
@frontend/lib/contracts/index.ts
@backend/tests/test_contract_alignment.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Expand health score contract for explainability factors</name>
  <files>contracts/v1/health.py, frontend/lib/contracts/index.ts, backend/tests/test_contract_alignment.py</files>
  <read_first>
    - contracts/v1/health.py
    - frontend/lib/contracts/index.ts
    - backend/tests/test_contract_alignment.py
    - backend/app/domain/contracts/__init__.py
  </read_first>
  <action>
    Per HEALTH-03, extend backend and frontend health contracts with explicit factor-level explainability fields.

    In `contracts/v1/health.py`:
    - Add `HealthFactor` model with fields:
      - `name: str`
      - `weight: float = Field(ge=0, le=1)`
      - `raw_value: float`
      - `normalized_value: float = Field(ge=0, le=1)`
      - `direction: str = Field(pattern="^(positive|negative)$")`
    - Update `HealthScore` to include:
      - existing `component_id`, `score`, `contributors`, `measured_at`
      - new `run_id: str`
      - new `repository_id: str`
      - new `factors: list[HealthFactor]`

    In `frontend/lib/contracts/index.ts`:
    - Add `HealthFactor` interface with matching fields and types.
    - Update `HealthScore` interface with `run_id`, `repository_id`, and `factors: HealthFactor[]`.

    In `backend/tests/test_contract_alignment.py`:
    - Keep existing loop-based field checks.
    - Add assertions that frontend source contains `export interface HealthFactor` and `factors: HealthFactor[]`.
  </action>
  <acceptance_criteria>
    - `contracts/v1/health.py` contains `class HealthFactor(BaseModel):`
    - `contracts/v1/health.py` contains `factors: list[HealthFactor]`
    - `contracts/v1/health.py` contains `direction: str = Field(pattern="^(positive|negative)$")`
    - `frontend/lib/contracts/index.ts` contains `export interface HealthFactor {`
    - `frontend/lib/contracts/index.ts` contains `factors: HealthFactor[];`
    - `backend/tests/test_contract_alignment.py` contains assertion for `export interface HealthFactor`
  </acceptance_criteria>
  <verify>
    <automated>pytest backend/tests/test_contract_alignment.py -q</automated>
  </verify>
  <done>Backend and frontend contracts include factor-level explainability fields and alignment test passes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement Radon metrics extraction with deterministic fallback</name>
  <files>backend/tests/test_health_metrics.py, backend/app/domain/health/metrics.py</files>
  <read_first>
    - backend/tests/test_provider_normalization.py
    - backend/app/domain/ingestion/normalization.py
    - backend/tests/test_health.py
  </read_first>
  <action>
    Per HEALTH-01, implement a domain metric extractor that computes per-file cyclomatic complexity and maintainability.

    Create `backend/tests/test_health_metrics.py` first (RED) with test cases for:
    - Python file payload returns metric entry containing `component_id`, `complexity`, `maintainability_index`.
    - Excluded paths (`node_modules`, `.venv`, `dist`, `build`, `__pycache__`) are not analyzed.
    - Missing Radon dependency path returns deterministic fallback metric and contributor marker `radon_unavailable`.

    Then create `backend/app/domain/health/metrics.py` with:
    - `collect_python_files(artifacts: list[dict]) -> list[str]` that filters `.py` files and excludes generated/vendor paths.
    - `compute_radon_metrics(component_files: list[str]) -> list[dict]` that attempts Radon import and computes:
      - `component_id` (file path)
      - `complexity` (float)
      - `maintainability_index` (float)
      - `contributors` (list[str])
    - fallback behavior when Radon import fails or parsing fails:
      - complexity=0.0, maintainability_index=100.0, contributor includes `radon_unavailable` or `analysis_error`.
  </action>
  <acceptance_criteria>
    - `backend/app/domain/health/metrics.py` contains `def collect_python_files(`
    - `backend/app/domain/health/metrics.py` contains `def compute_radon_metrics(`
    - `backend/app/domain/health/metrics.py` contains exclusion checks for `node_modules` and `__pycache__`
    - `backend/app/domain/health/metrics.py` contains fallback marker `radon_unavailable`
    - `backend/tests/test_health_metrics.py` exists and includes at least 3 test functions
  </acceptance_criteria>
  <verify>
    <automated>pytest backend/tests/test_health_metrics.py -q</automated>
  </verify>
  <done>Metrics extraction works for Python files and deterministically degrades when Radon is unavailable.</done>
</task>

</tasks>

<verification>
Run focused checks:
- `pytest backend/tests/test_contract_alignment.py -q`
- `pytest backend/tests/test_health_metrics.py -q`
</verification>

<success_criteria>
- HEALTH-01 extraction primitives exist and pass tests.
- HEALTH-03 contract fields for explainability are present in both backend and frontend contracts.
</success_criteria>

<output>
After completion, create `.planning/phases/03-code-health-scoring-engine/03-01-SUMMARY.md`
</output>
