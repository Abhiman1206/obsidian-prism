---
phase: 02-repository-ingestion-and-evidence-graph
plan: 03
type: execute
wave: 3
depends_on:
  - 02-01
  - 02-02
files_modified:
  - backend/app/infra/miners/pydriller_miner.py
  - backend/app/domain/ingestion/checkpoints.py
  - backend/app/domain/ingestion/persistence.py
  - backend/app/workers/incremental_ingestion.py
  - backend/tests/test_pydriller_incremental.py
  - backend/tests/test_ingestion_checkpoint_resume.py
autonomous: true
requirements:
  - INGEST-05
must_haves:
  truths:
    - Ingestion jobs resume from the last successful checkpoint without full-history reprocessing
    - Incremental mining captures only unseen commits and changed files since the checkpoint
    - Checkpoint progression is deterministic and test-validated
  artifacts:
    - path: backend/app/infra/miners/pydriller_miner.py
      provides: incremental PyDriller mining utility
    - path: backend/app/domain/ingestion/checkpoints.py
      provides: checkpoint read/write and resume decision logic
    - path: backend/app/workers/incremental_ingestion.py
      provides: end-to-end incremental ingestion orchestration
  key_links:
    - from: backend/app/workers/incremental_ingestion.py
      to: backend/app/domain/ingestion/checkpoints.py
      via: checkpoint load/save calls
      pattern: checkpoint
    - from: backend/app/workers/incremental_ingestion.py
      to: backend/app/infra/miners/pydriller_miner.py
      via: incremental mining invocation
      pattern: mine_incremental
---

<objective>
Implement PyDriller-based incremental mining and checkpoint resume behavior for ingestion jobs.

Purpose: Satisfy INGEST-05 by ensuring ingestion can resume safely and efficiently without reprocessing complete repository history.
Output: Incremental miner, checkpoint services, orchestration worker, and resume tests.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-repository-ingestion-and-evidence-graph/02-RESEARCH.md
@.planning/phases/02-repository-ingestion-and-evidence-graph/02-02-PLAN.md

<interfaces>
Incremental checkpoint contract expected by this plan:
- repository_id: string
- provider: "github" | "gitlab"
- last_processed_commit_sha: string | null
- last_processed_at: string | null
- status: "idle" | "running" | "failed" | "complete"

Incremental miner output should include commits newer than checkpoint and changed file metadata for each commit.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement PyDriller incremental miner and checkpoint primitives</name>
  <files>backend/app/infra/miners/pydriller_miner.py, backend/app/domain/ingestion/checkpoints.py</files>
  <behavior>
    - Miner accepts checkpoint boundaries and only returns unseen commits
    - Checkpoint service can persist and load last processed marker per repository
    - Empty incremental window returns stable empty result, not failure
  </behavior>
  <action>Implement incremental PyDriller miner and checkpoint primitives that track last processed commit/time per repository/provider pair. Ensure replay-safe checkpoint updates after successful mining batches.</action>
  <verify>
    <automated>cd backend; python -m pytest tests/test_pydriller_incremental.py tests/test_ingestion_checkpoint_resume.py -q</automated>
  </verify>
  <done>Incremental miner and checkpoint primitives pass resume and empty-window test scenarios.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire incremental ingestion worker and persistence boundary</name>
  <files>backend/app/domain/ingestion/persistence.py, backend/app/workers/incremental_ingestion.py, backend/tests/test_pydriller_incremental.py, backend/tests/test_ingestion_checkpoint_resume.py</files>
  <behavior>
    - Worker loads checkpoint, mines incremental commits, persists artifacts, then advances checkpoint
    - Failure path does not advance checkpoint
    - Resume run continues from previous successful marker
  </behavior>
  <action>Implement incremental ingestion worker orchestration around checkpoint load/mine/persist/save flow. Add persistence abstraction for mined artifacts to keep storage concerns separate from miner logic.</action>
  <verify>
    <automated>cd backend; python -m pytest tests/test_pydriller_incremental.py tests/test_ingestion_checkpoint_resume.py -q</automated>
  </verify>
  <done>Worker flow demonstrates deterministic checkpoint resume semantics and guarded checkpoint advancement.</done>
</task>

</tasks>

<verification>
- Incremental mining path skips already-processed history.
- Checkpoint does not advance on failure.
- Resume run continues from last successful marker.
</verification>

<success_criteria>
- INGEST-05 is satisfied with deterministic checkpoint-based incremental ingestion.
- Incremental ingestion output is ready for lineage persistence in the next plan.
</success_criteria>

<output>
After completion, create `.planning/phases/02-repository-ingestion-and-evidence-graph/02-03-SUMMARY.md`
</output>
