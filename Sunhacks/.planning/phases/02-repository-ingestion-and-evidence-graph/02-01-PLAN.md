---
phase: 02-repository-ingestion-and-evidence-graph
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/api/schemas/provider_auth.py
  - backend/app/api/schemas/repository.py
  - backend/app/api/routes/repositories.py
  - backend/app/domain/repositories/registry.py
  - backend/app/infra/secrets/provider_credentials.py
  - backend/app/main.py
  - backend/tests/test_provider_auth_contract.py
  - backend/tests/test_repository_registration_contract.py
autonomous: true
requirements:
  - INGEST-01
  - INGEST-02
user_setup:
  - service: github
    why: "OAuth app and token scopes are required for repository authorization."
    env_vars:
      - name: GITHUB_CLIENT_ID
        source: "GitHub Developer Settings -> OAuth Apps"
      - name: GITHUB_CLIENT_SECRET
        source: "GitHub Developer Settings -> OAuth Apps"
  - service: gitlab
    why: "OAuth app and token scopes are required for repository authorization."
    env_vars:
      - name: GITLAB_CLIENT_ID
        source: "GitLab User Settings -> Applications"
      - name: GITLAB_CLIENT_SECRET
        source: "GitLab User Settings -> Applications"
must_haves:
  truths:
    - User can register a GitHub repository for analysis with validated provider auth payloads
    - User can register a GitLab repository for analysis with validated provider auth payloads
    - Registration responses return typed repository identity and run-readiness metadata
  artifacts:
    - path: backend/app/api/routes/repositories.py
      provides: provider-specific repository registration endpoints
    - path: backend/app/api/schemas/repository.py
      provides: typed request and response contracts for repository registration
    - path: backend/app/domain/repositories/registry.py
      provides: repository registration service boundary
  key_links:
    - from: backend/app/api/routes/repositories.py
      to: backend/app/domain/repositories/registry.py
      via: route-to-service registration call
      pattern: register_repository
    - from: backend/app/api/schemas/provider_auth.py
      to: backend/app/api/routes/repositories.py
      via: request validation model
      pattern: provider|token|scope
---

<objective>
Implement provider auth/config payload contracts and repository registration APIs for GitHub and GitLab.

Purpose: Satisfy INGEST-01 and INGEST-02 with typed, testable registration endpoints that downstream ingestion plans can depend on.
Output: Registration routes, provider auth schemas, and contract tests.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-repository-ingestion-and-evidence-graph/02-RESEARCH.md
@.planning/phases/01-platform-foundation-and-contracts/01-01-SUMMARY.md
@.planning/phases/01-platform-foundation-and-contracts/01-03-SUMMARY.md

<interfaces>
Existing run contract semantics from backend/app/api/schemas/run.py:
- provider pattern: ^(github|gitlab)$
- status enum values: queued|running|succeeded|failed

New repository registration contracts should follow the same strict schema pattern and explicit enum validation.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define provider auth and repository registration API contracts</name>
  <files>backend/app/api/schemas/provider_auth.py, backend/app/api/schemas/repository.py</files>
  <behavior>
    - Request schema accepts only github or gitlab providers
    - Token/scope fields are explicit and validated
    - Response schema includes repository_id, provider, repository_url, and authorization status
  </behavior>
  <action>Create provider auth and repository registration schemas with strict field typing and explicit validation constraints. Keep field names canonical for downstream ingestion adapters.</action>
  <verify>
    <automated>cd backend; python -m pytest tests/test_provider_auth_contract.py -q</automated>
  </verify>
  <done>Typed schemas compile and tests confirm validation behavior for GitHub and GitLab payloads.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement repository registration routes and service wiring</name>
  <files>backend/app/api/routes/repositories.py, backend/app/domain/repositories/registry.py, backend/app/infra/secrets/provider_credentials.py, backend/app/main.py</files>
  <behavior>
    - POST endpoint registers provider repository with validated payload
    - Route delegates persistence logic to registry service
    - Credential handling path is isolated behind infra abstraction
  </behavior>
  <action>Implement repository registration route handlers for github and gitlab providers, wire them in app.main, and route credential/token handling through provider_credentials abstraction instead of embedding token logic in API routes.</action>
  <verify>
    <automated>cd backend; python -m pytest tests/test_repository_registration_contract.py -q</automated>
  </verify>
  <done>Repository registration endpoint returns typed payload and route-to-service boundary is enforced.</done>
</task>

<task type="auto">
  <name>Task 3: Add API contract tests for provider registration</name>
  <files>backend/tests/test_provider_auth_contract.py, backend/tests/test_repository_registration_contract.py</files>
  <action>Add endpoint and schema tests covering provider validation, missing scope failures, and success responses for both github and gitlab providers.</action>
  <verify>
    <automated>cd backend; python -m pytest tests/test_provider_auth_contract.py tests/test_repository_registration_contract.py -q</automated>
  </verify>
  <done>Automated tests enforce contract correctness for repository registration behavior.</done>
</task>

</tasks>

<verification>
- Provider auth schemas reject invalid providers and malformed tokens.
- Repository registration endpoints are mounted and return typed response models.
- Backend tests for registration contracts pass.
</verification>

<success_criteria>
- INGEST-01 and INGEST-02 are satisfied with typed, test-backed registration APIs.
- Phase 2 downstream plans can consume canonical repository registration outputs.
</success_criteria>

<output>
After completion, create `.planning/phases/02-repository-ingestion-and-evidence-graph/02-01-SUMMARY.md`
</output>
