---
phase: 02-repository-ingestion-and-evidence-graph
plan: 04
type: execute
wave: 4
depends_on:
  - 02-02
  - 02-03
files_modified:
  - backend/app/domain/evidence/schema.py
  - backend/app/domain/evidence/lineage_writer.py
  - backend/app/domain/evidence/repository.py
  - backend/app/workers/lineage_ingestion.py
  - backend/app/api/routes/lineage.py
  - backend/app/main.py
  - backend/tests/test_evidence_lineage.py
  - backend/tests/test_lineage_route_contract.py
autonomous: true
requirements:
  - AUDIT-01
must_haves:
  truths:
    - Ingestion artifacts are persisted with lineage links that identify source and processing step
    - Lineage entries can be retrieved by run or repository for audit inspection
    - Every persisted lineage record includes deterministic claim and source references
  artifacts:
    - path: backend/app/domain/evidence/schema.py
      provides: evidence graph entity schemas for ingestion lineage
    - path: backend/app/domain/evidence/lineage_writer.py
      provides: lineage write service for ingestion artifacts
    - path: backend/app/api/routes/lineage.py
      provides: API retrieval endpoint for lineage trace lookups
  key_links:
    - from: backend/app/workers/lineage_ingestion.py
      to: backend/app/domain/evidence/lineage_writer.py
      via: lineage persistence call
      pattern: write_lineage
    - from: backend/app/api/routes/lineage.py
      to: backend/app/domain/evidence/repository.py
      via: lineage query boundary
      pattern: get_lineage
---

<objective>
Implement evidence graph schema and lineage writer for ingestion artifacts.

Purpose: Satisfy AUDIT-01 by making ingestion-stage outputs traceable with deterministic source and claim links.
Output: Evidence schema, lineage writer/query interfaces, route exposure, and lineage tests.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-repository-ingestion-and-evidence-graph/02-RESEARCH.md
@.planning/phases/02-repository-ingestion-and-evidence-graph/02-03-PLAN.md

<interfaces>
Lineage record contract targeted by this plan:
- lineage_id: string
- run_id: string
- repository_id: string
- artifact_type: "commit" | "churn" | "cadence"
- artifact_id: string
- source_provider: "github" | "gitlab" | "pydriller"
- source_locator: string
- claim_ref: string
- created_at: string

Route contract:
- GET /api/lineage/{run_id} returns list of lineage records for the run.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define evidence graph schema and lineage persistence service</name>
  <files>backend/app/domain/evidence/schema.py, backend/app/domain/evidence/lineage_writer.py, backend/app/domain/evidence/repository.py</files>
  <behavior>
    - Evidence schema includes deterministic IDs and source references
    - Lineage writer persists one record per ingestion artifact with claim_ref linkage
    - Repository interface supports query by run_id and repository_id
  </behavior>
  <action>Create evidence schema and lineage writer/repository interfaces for ingestion artifacts. Ensure required source and claim fields are mandatory to enforce traceability guarantees.</action>
  <verify>
    <automated>cd backend; python -m pytest tests/test_evidence_lineage.py -q</automated>
  </verify>
  <done>Evidence schema and writer API support deterministic lineage writes and lookup paths.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire lineage ingestion worker and API route exposure</name>
  <files>backend/app/workers/lineage_ingestion.py, backend/app/api/routes/lineage.py, backend/app/main.py, backend/tests/test_lineage_route_contract.py</files>
  <behavior>
    - Worker receives normalized ingestion payload and writes lineage events
    - API route returns lineage entries by run_id in typed response
    - Route is registered in app.main and validates run identifier inputs
  </behavior>
  <action>Implement lineage_ingestion worker and lineage API route, wire route registration in app.main, and add endpoint contract tests for successful and empty lineage responses.</action>
  <verify>
    <automated>cd backend; python -m pytest tests/test_evidence_lineage.py tests/test_lineage_route_contract.py -q</automated>
  </verify>
  <done>Lineage records are written during ingestion and are retrievable via typed API contract.</done>
</task>

</tasks>

<verification>
- Lineage records exist for commit/churn/cadence artifacts.
- Lineage lookup route returns deterministic source and claim references.
- Lineage tests pass for write and read paths.
</verification>

<success_criteria>
- AUDIT-01 is satisfied for ingestion-stage artifacts with deterministic evidence traceability.
- Phase 2 outputs are audit-ready inputs for health, risk, and business synthesis phases.
</success_criteria>

<output>
After completion, create `.planning/phases/02-repository-ingestion-and-evidence-graph/02-04-SUMMARY.md`
</output>
