---
phase: 02-repository-ingestion-and-evidence-graph
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - backend/app/infra/providers/base.py
  - backend/app/infra/providers/github_client.py
  - backend/app/infra/providers/gitlab_client.py
  - backend/app/domain/ingestion/normalization.py
  - backend/app/domain/ingestion/provider_signals.py
  - backend/app/workers/provider_ingestion.py
  - backend/tests/test_provider_normalization.py
  - backend/tests/test_provider_signals.py
autonomous: true
requirements:
  - INGEST-03
  - INGEST-04
must_haves:
  truths:
    - System ingests commit history and changed files from connected providers into a canonical shape
    - System captures issue and deployment cadence signals when provider APIs expose them
    - Provider-specific differences are normalized before downstream persistence
  artifacts:
    - path: backend/app/infra/providers/github_client.py
      provides: GitHub ingestion adapter with pagination handling
    - path: backend/app/infra/providers/gitlab_client.py
      provides: GitLab ingestion adapter with pagination handling
    - path: backend/app/domain/ingestion/normalization.py
      provides: provider-to-canonical mapping functions
  key_links:
    - from: backend/app/workers/provider_ingestion.py
      to: backend/app/infra/providers/github_client.py
      via: provider adapter dispatch
      pattern: github
    - from: backend/app/workers/provider_ingestion.py
      to: backend/app/infra/providers/gitlab_client.py
      via: provider adapter dispatch
      pattern: gitlab
    - from: backend/app/infra/providers/*.py
      to: backend/app/domain/ingestion/normalization.py
      via: canonical transformation pipeline
      pattern: normalize
---

<objective>
Build provider ingestion adapters and normalization flow for GitHub/GitLab commit and cadence signals.

Purpose: Satisfy INGEST-03 and INGEST-04 with provider adapters that collect commit/churn data plus cadence signals in a canonical format.
Output: Provider clients, normalization functions, ingestion worker orchestration, and adapter tests.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-repository-ingestion-and-evidence-graph/02-RESEARCH.md
@.planning/phases/02-repository-ingestion-and-evidence-graph/02-01-PLAN.md

<interfaces>
Expected canonical ingestion output shape (used by downstream plans):
- repository_id: string
- provider: "github" | "gitlab"
- commits: [{ commit_sha, authored_at, author_email, files_changed, insertions, deletions }]
- churn: [{ contributor, commits_last_30d, files_touched_last_30d }]
- cadence: { issue_opened_count, issue_closed_count, deployment_count, period_start, period_end }
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement provider client adapters with pagination and timeout boundaries</name>
  <files>backend/app/infra/providers/base.py, backend/app/infra/providers/github_client.py, backend/app/infra/providers/gitlab_client.py</files>
  <behavior>
    - Adapter clients page through provider APIs until completion
    - Shared timeout and error boundaries are enforced via base adapter
    - Returned payloads include commit and file-level details needed for churn metrics
  </behavior>
  <action>Implement base provider adapter abstraction and concrete github/gitlab clients that fetch commit history and changed file metadata with pagination support. Enforce deterministic timeout boundaries through the base client contract.</action>
  <verify>
    <automated>cd backend; python -m pytest tests/test_provider_normalization.py -q</automated>
  </verify>
  <done>Provider adapters return deterministic raw payload batches and handle pagination correctly.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add canonical normalization and cadence signal extraction</name>
  <files>backend/app/domain/ingestion/normalization.py, backend/app/domain/ingestion/provider_signals.py</files>
  <behavior>
    - GitHub and GitLab payloads are transformed to the same canonical schema
    - Issue/deployment cadence metrics are extracted when API data is available
    - Missing optional provider signals do not fail ingestion
  </behavior>
  <action>Create canonical mapping functions for commits/files/churn and add provider cadence extraction logic for issue/deployment signals. Keep optional fields nullable and avoid hard failure on unsupported provider endpoints.</action>
  <verify>
    <automated>cd backend; python -m pytest tests/test_provider_normalization.py tests/test_provider_signals.py -q</automated>
  </verify>
  <done>Normalization and cadence extraction produce canonical ingestion payloads across both providers.</done>
</task>

<task type="auto">
  <name>Task 3: Wire provider ingestion orchestration entrypoint</name>
  <files>backend/app/workers/provider_ingestion.py, backend/tests/test_provider_normalization.py, backend/tests/test_provider_signals.py</files>
  <action>Implement provider_ingestion worker entrypoint that dispatches by provider, calls adapters, applies normalization/signals, and returns one canonical ingestion payload for downstream persistence stages.</action>
  <verify>
    <automated>cd backend; python -m pytest tests/test_provider_normalization.py tests/test_provider_signals.py -q</automated>
  </verify>
  <done>Worker orchestration path exists and is validated by adapter and normalization tests.</done>
</task>

</tasks>

<verification>
- GitHub and GitLab adapters fetch paginated commit/file data.
- Canonical normalization and cadence extraction paths pass tests.
- Ingestion worker emits unified provider-agnostic payload shape.
</verification>

<success_criteria>
- INGEST-03 and INGEST-04 are satisfied through tested provider adapters and canonical normalization.
- Downstream incremental mining and lineage plans can consume normalized outputs without provider branching.
</success_criteria>

<output>
After completion, create `.planning/phases/02-repository-ingestion-and-evidence-graph/02-02-SUMMARY.md`
</output>
