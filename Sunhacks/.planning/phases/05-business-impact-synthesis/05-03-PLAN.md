---
phase: 05-business-impact-synthesis
plan: 03
type: execute
wave: 3
depends_on: [05-02]
files_modified:
  - backend/tests/test_executive_reports_route_contract.py
  - backend/app/domain/business/repository.py
  - backend/app/workers/business_reporting.py
  - backend/app/api/routes/executive_reports.py
  - backend/app/main.py
autonomous: true
requirements: [BIZ-03]
must_haves:
  truths:
    - "Executive reports expose cost-of-inaction and priority recommendations with run-scoped retrieval"
    - "Each report claim includes machine-readable lineage references for auditability"
    - "Unknown run report lookups return stable empty-list behavior"
  artifacts:
    - path: "backend/app/domain/business/repository.py"
      provides: "run-scoped executive report persistence and retrieval"
    - path: "backend/app/api/routes/executive_reports.py"
      provides: "GET API for executive report payload and evidence-linked claims"
    - path: "backend/tests/test_executive_reports_route_contract.py"
      provides: "route contract verification for report claims and lineage refs"
  key_links:
    - from: "backend/app/workers/business_reporting.py"
      to: "backend/app/domain/business/repository.py"
      via: "worker persistence helper writes report payloads"
    - from: "backend/app/api/routes/executive_reports.py"
      to: "backend/app/workers/lineage_ingestion.py"
      via: "route claim lineage_refs align with run-scoped lineage records"
---

<objective>
Persist executive report outputs and expose run-scoped API retrieval with claim-level evidence lineage references.

Purpose: Complete BIZ-03 by making cost-of-inaction recommendations auditable through explicit claim trace links.
Output: Report repository, API route wiring, and contract-tested claim-to-lineage retrieval behavior.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-business-impact-synthesis/05-02-PLAN.md
@backend/app/domain/evidence/repository.py
@backend/app/api/routes/lineage.py
@backend/app/main.py

<interfaces>
From backend/app/domain/evidence/repository.py:
- get_lineage(run_id=...) returns lineage records for the run.

From 05-02 planned output:
- run_business_reporting(...) returns report payload with section-level claims and recommendations.

From backend/app/api/routes/lineage.py:
- API pattern for unknown run returns [] with 200 status.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define failing route contract tests for executive report retrieval with claim lineage references</name>
  <files>backend/tests/test_executive_reports_route_contract.py</files>
  <read_first>
    - backend/tests/test_lineage_route_contract.py
    - backend/tests/test_risk_forecasts_route_contract.py
    - backend/app/api/routes/lineage.py
    - backend/app/main.py
  </read_first>
  <behavior>
    - GET /api/executive-reports/{run_id} returns report payload list sorted by generated_at desc.
    - Each report claim contains claim_id, claim_text, and lineage_refs list.
    - Unknown run returns HTTP 200 with an empty list.
  </behavior>
  <action>
    Add RED tests in backend/tests/test_executive_reports_route_contract.py:
    - Seed report records with claim objects that include lineage_refs.
    - Assert required fields for executive_summary, cost_of_inaction, recommended_priorities, and claims.
    - Assert unknown run behavior returns empty list with status 200.
  </action>
  <verify>
    <automated>pytest backend/tests/test_executive_reports_route_contract.py -q</automated>
  </verify>
  <done>Failing tests define BIZ-03 route contract for evidence-linked report claims.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement report repository, persistence helper, route, and main app wiring</name>
  <files>backend/app/domain/business/repository.py, backend/app/workers/business_reporting.py, backend/app/api/routes/executive_reports.py, backend/app/main.py</files>
  <read_first>
    - backend/tests/test_executive_reports_route_contract.py
    - backend/app/domain/evidence/repository.py
    - backend/app/api/routes/lineage.py
  </read_first>
  <action>
    Implement run-scoped report retrieval with claim lineage references:
    - Create backend/app/domain/business/repository.py with singleton ExecutiveReportRepository supporting add(record) and get_by_run(run_id).
    - Update backend/app/workers/business_reporting.py with persist_executive_report(report: dict) helper.
    - Create backend/app/api/routes/executive_reports.py exposing GET /api/executive-reports/{run_id} returning repository data.
    - Ensure report claims include claim_id, claim_text, lineage_refs in persisted payload.
    - Register executive_reports router in backend/app/main.py.
  </action>
  <verify>
    <automated>pytest backend/tests/test_executive_reports_route_contract.py -q</automated>
  </verify>
  <done>Executive reports are retrievable by run with claim-level lineage references and stable empty-run behavior.</done>
</task>

</tasks>

<verification>
- pytest backend/tests/test_executive_reports_route_contract.py -q
</verification>

<success_criteria>
- BIZ-03 is satisfied with run-scoped report retrieval and auditable claim lineage references.
- Cost-of-inaction and prioritized recommendations are exposed through a stable API contract.
</success_criteria>

<output>
After completion, create .planning/phases/05-business-impact-synthesis/05-03-SUMMARY.md
</output>
