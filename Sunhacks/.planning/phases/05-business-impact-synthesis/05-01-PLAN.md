---
phase: 05-business-impact-synthesis
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/tests/test_business_impact_translation.py
  - backend/app/domain/business/__init__.py
  - backend/app/domain/business/translation.py
autonomous: true
requirements: [BIZ-01]
must_haves:
  truths:
    - "Technical component risk is translated into deterministic time and monetary impact values"
    - "Translation output supports configurable business assumptions with stable defaults"
    - "Identical inputs produce identical business impact output ordering and values"
  artifacts:
    - path: "backend/app/domain/business/translation.py"
      provides: "deterministic risk-to-business KPI translation formulas"
    - path: "backend/tests/test_business_impact_translation.py"
      provides: "behavior coverage for configurable assumptions and financial outputs"
  key_links:
    - from: "backend/app/domain/business/translation.py"
      to: "backend/app/domain/risk/repository.py"
      via: "consumes ranked risk forecast rows as translation input"
    - from: "backend/app/domain/business/translation.py"
      to: "contracts/v1/report.py"
      via: "output shape aligns with executive report financial fields"
---

<objective>
Create the business assumption model and deterministic KPI translation engine for converting risk forecasts into estimated financial impact.

Purpose: Implement BIZ-01 so report generation can rely on stable, explainable business-cost calculations.
Output: Deterministic translation domain module with passing tests for assumptions and cost outputs.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-90-day-predictive-risk-forecasting/04-03-SUMMARY.md
@backend/app/domain/risk/repository.py
@contracts/v1/report.py

<interfaces>
From backend/app/domain/risk/repository.py:
- get_ranked_by_run(run_id) returns forecast rows with risk_probability and confidence.

From contracts/v1/report.py (current):
- ExecutiveReportSummary currently includes report_id, run_id, executive_summary, cost_of_inaction_estimate, generated_at.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define failing tests for deterministic business-impact translation</name>
  <files>backend/tests/test_business_impact_translation.py</files>
  <read_first>
    - backend/tests/test_risk_forecasting.py
    - backend/tests/test_risk_forecasts_route_contract.py
    - backend/app/domain/risk/forecasting.py
    - backend/app/domain/risk/repository.py
  </read_first>
  <behavior>
    - Translation output contains expected_engineering_hours, expected_downtime_hours, and expected_total_cost.
    - Business assumptions are configurable while preserving deterministic outputs for identical input.
    - Output is sorted by expected_total_cost descending then component_id ascending.
  </behavior>
  <action>
    Create RED tests in backend/tests/test_business_impact_translation.py that specify BIZ-01 behavior:
    - Define fixture risk rows with varying probability and confidence.
    - Define default assumptions and override assumptions.
    - Assert expected_engineering_hours, expected_downtime_hours, and expected_total_cost are present and non-negative.
    - Assert deterministic ordering by expected_total_cost desc and component_id asc.
  </action>
  <verify>
    <automated>pytest backend/tests/test_business_impact_translation.py -q</automated>
  </verify>
  <done>Failing tests codify deterministic business translation behavior with configurable assumptions.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement business assumption defaults and KPI translation logic</name>
  <files>backend/app/domain/business/translation.py, backend/app/domain/business/__init__.py</files>
  <read_first>
    - backend/tests/test_business_impact_translation.py
    - backend/app/domain/risk/repository.py
    - contracts/v1/report.py
  </read_first>
  <action>
    Implement deterministic translation logic in backend/app/domain/business/translation.py:
    - Add DEFAULT_BUSINESS_ASSUMPTIONS with explicit numeric defaults.
    - Add translate_risk_to_business_impact(risk_rows: list[dict], assumptions: dict | None = None) -> list[dict].
    - Compute expected_engineering_hours, expected_downtime_hours, and expected_total_cost from forecast probability, confidence, and assumptions.
    - Include a cost_drivers list with top financial driver labels for explainability.
    - Return rows sorted by expected_total_cost desc then component_id asc.
    - Export translation function from backend/app/domain/business/__init__.py.
  </action>
  <verify>
    <automated>pytest backend/tests/test_business_impact_translation.py -q</automated>
  </verify>
  <done>Business translation engine deterministically converts risk rows into configurable financial impact outputs.</done>
</task>

</tasks>

<verification>
- pytest backend/tests/test_business_impact_translation.py -q
</verification>

<success_criteria>
- BIZ-01 translation output includes deterministic time and cost estimates per component.
- Assumption overrides alter outputs predictably without breaking ordering or shape.
</success_criteria>

<output>
After completion, create .planning/phases/05-business-impact-synthesis/05-01-SUMMARY.md
</output>
