---
phase: 05-business-impact-synthesis
plan: 02
type: execute
wave: 2
depends_on: [05-01]
files_modified:
  - backend/tests/test_executive_report_writer.py
  - backend/tests/test_contract_alignment.py
  - contracts/v1/report.py
  - frontend/lib/contracts/index.ts
  - backend/app/domain/business/report_writer.py
  - backend/app/workers/business_reporting.py
autonomous: true
requirements: [BIZ-02]
must_haves:
  truths:
    - "Executive report narrative is plain-language and action-oriented"
    - "Report output includes prioritized recommendations tied to financial impact"
    - "Backend and frontend report contracts remain aligned after narrative schema expansion"
  artifacts:
    - path: "backend/app/domain/business/report_writer.py"
      provides: "deterministic executive narrative section generation"
    - path: "contracts/v1/report.py"
      provides: "typed executive report contract with narrative and recommendation sections"
    - path: "backend/app/workers/business_reporting.py"
      provides: "worker orchestration from translated impact rows to report payload"
  key_links:
    - from: "backend/app/domain/business/report_writer.py"
      to: "backend/app/domain/business/translation.py"
      via: "consumes translated impact rows from 05-01"
    - from: "contracts/v1/report.py"
      to: "frontend/lib/contracts/index.ts"
      via: "contract alignment test enforces mirrored report fields"
---

<objective>
Implement the executive report writer templates and typed report contract to produce CEO-readable, action-focused narratives.

Purpose: Implement BIZ-02 with deterministic plain-language report sections and prioritized recommendations.
Output: Report-writing domain module, worker wrapper, and synced report contracts.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-business-impact-synthesis/05-01-PLAN.md
@contracts/v1/report.py
@frontend/lib/contracts/index.ts

<interfaces>
From 05-01 planned output:
- translate_risk_to_business_impact(...) returns per-component financial impact rows sorted by expected_total_cost.

From backend/tests/test_contract_alignment.py:
- Contract parity checks validate Python contract fields exist in frontend report interfaces.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define failing tests for plain-language executive report narrative and contract parity</name>
  <files>backend/tests/test_executive_report_writer.py, backend/tests/test_contract_alignment.py</files>
  <read_first>
    - backend/tests/test_business_impact_translation.py
    - backend/tests/test_contract_alignment.py
    - contracts/v1/report.py
    - frontend/lib/contracts/index.ts
  </read_first>
  <behavior>
    - Report writer returns executive_summary, top_risks, cost_of_inaction, and recommended_priorities sections.
    - Narrative avoids engineering jargon tokens and uses action-oriented recommendation phrasing.
    - Report contract fields are mirrored in frontend TypeScript contract definitions.
  </behavior>
  <action>
    Add RED tests in backend/tests/test_executive_report_writer.py and extend backend/tests/test_contract_alignment.py:
    - Define narrative expectations from deterministic translated impact rows.
    - Assert recommendation ordering follows expected_total_cost priority.
    - Assert disallowed jargon tokens (for example cyclomatic, maintainability, refactor) are absent from executive_summary.
    - Extend contract alignment assertions for new report section fields.
  </action>
  <verify>
    <automated>pytest backend/tests/test_executive_report_writer.py backend/tests/test_contract_alignment.py -q</automated>
  </verify>
  <done>Failing tests define plain-language report behavior and backend/frontend contract parity requirements.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement report writer templates, worker orchestration, and report contract updates</name>
  <files>contracts/v1/report.py, frontend/lib/contracts/index.ts, backend/app/domain/business/report_writer.py, backend/app/workers/business_reporting.py</files>
  <read_first>
    - backend/tests/test_executive_report_writer.py
    - backend/tests/test_contract_alignment.py
    - backend/app/domain/business/translation.py
  </read_first>
  <action>
    Implement BIZ-02 report generation flow:
    - Expand contracts/v1/report.py with section-level models for top_risks, cost_of_inaction, and recommended_priorities while preserving ExecutiveReportSummary compatibility.
    - Mirror the same shape in frontend/lib/contracts/index.ts.
    - Create backend/app/domain/business/report_writer.py with a deterministic write_executive_report(run_id: str, translated_rows: list[dict]) -> dict function.
    - Create backend/app/workers/business_reporting.py with run_business_reporting(run_id: str, translated_rows: list[dict]) -> dict wrapper that adds generated_at timestamp and report_id.
    - Ensure recommendation ordering follows financial impact priority and narrative remains jargon-light.
  </action>
  <verify>
    <automated>pytest backend/tests/test_executive_report_writer.py backend/tests/test_contract_alignment.py -q</automated>
  </verify>
  <done>Executive report writer produces plain-language, action-oriented narratives with aligned contracts.</done>
</task>

</tasks>

<verification>
- pytest backend/tests/test_executive_report_writer.py backend/tests/test_contract_alignment.py -q
</verification>

<success_criteria>
- BIZ-02 is satisfied with deterministic, executive-safe narrative output.
- Report contracts remain synchronized across Python and frontend TypeScript.
</success_criteria>

<output>
After completion, create .planning/phases/05-business-impact-synthesis/05-02-SUMMARY.md
</output>
