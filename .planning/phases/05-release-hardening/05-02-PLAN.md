---
phase: 05-release-hardening
plan: 02
type: execute
wave: 2
depends_on: [05-01]
files_modified:
  - Dockerfile
  - .dockerignore
  - README.md
  - .planning/phases/05-release-hardening/05-RELEASE-CHECKLIST.md
autonomous: true
requirements: [QUAL-04]
must_haves:
  truths:
    - "Project can be built into a container image that starts the runtime service path"
    - "Container build uses deterministic dependency installation from locked files"
    - "Release process includes explicit smoke checks and rollback basics"
  artifacts:
    - path: "Dockerfile"
      provides: "Containerized runtime definition with deterministic dependency install and startup command"
    - path: ".dockerignore"
      provides: "Lean reproducible build context excluding caches and local artifacts"
    - path: ".planning/phases/05-release-hardening/05-RELEASE-CHECKLIST.md"
      provides: "Release smoke-test and rollback checklist"
  key_links:
    - from: "Dockerfile"
      to: "requirements.txt"
      via: "pip install at build time"
      pattern: "requirements\.txt"
    - from: "Dockerfile"
      to: "src/loan_agents/runtime/api.py"
      via: "container startup path for operational endpoints"
      pattern: "loan_agents\.runtime\.api"
    - from: ".planning/phases/05-release-hardening/05-RELEASE-CHECKLIST.md"
      to: "Dockerfile"
      via: "smoke and rollback commands reference built image"
      pattern: "docker build|docker run"
---

<objective>
Package runtime delivery artifacts for release readiness with deterministic container builds.

Purpose: Satisfy QUAL-04 by defining container runtime packaging and a repeatable release validation checklist.
Output: Dockerfile/.dockerignore plus release checklist documenting smoke tests and rollback basics.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/research/STACK.md
@src/loan_agents/runtime/api.py
@src/loan_agents/runtime/settings.py
@.planning/phases/05-release-hardening/05-01-PLAN.md

<interfaces>
From src/loan_agents/runtime/api.py:
```python
def health() -> dict[str, Any]
def readiness() -> dict[str, Any]
def run(request: dict[str, Any]) -> dict[str, Any]
```

From src/loan_agents/runtime/settings.py:
```python
def load_settings() -> RuntimeSettings
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add deterministic container runtime artifacts</name>
  <files>Dockerfile, .dockerignore</files>
  <action>Create a production-oriented Dockerfile that installs dependencies from locked requirements files produced in 05-01 (per QUAL-04), copies source, sets safe defaults, and starts the runtime entry path. Add .dockerignore to exclude .git, .planning, __pycache__, pytest cache, and local virtual environments for reproducible contexts. Avoid interactive prompts and avoid mutable build-time secret injection.</action>
  <verify>
    <automated>docker build -t loan-agents:phase5 .</automated>
  </verify>
  <done>Container image builds successfully from deterministic dependency inputs and includes runtime startup command.</done>
</task>

<task type="auto">
  <name>Task 2: Create release smoke/rollback checklist and wire docs to container flow</name>
  <files>.planning/phases/05-release-hardening/05-RELEASE-CHECKLIST.md, README.md</files>
  <action>Add a release checklist with concrete preflight, smoke test, and rollback basics: build image, run container with required env vars, call health/readiness/run paths, and rollback by redeploying last known-good tag. Update README deployment section to point to checklist and deterministic build/run commands. Keep steps non-interactive and scriptable.</action>
  <verify>
    <automated>python -m pytest tests/runtime/test_runtime_api_endpoints.py -q</automated>
  </verify>
  <done>Release documentation includes executable smoke-test and rollback basics tied to container artifacts.</done>
</task>

</tasks>

<verification>
- docker build -t loan-agents:phase5 .
- python -m pytest tests/runtime/test_runtime_api_endpoints.py -q
</verification>

<success_criteria>
- Container image builds reproducibly from locked dependencies and launches runtime entry path.
- Release checklist documents smoke tests for health/readiness/run and a practical rollback baseline.
- Delivery artifacts are sufficient for deployment handoff and operational validation.
</success_criteria>

<output>
After completion, create `.planning/phases/05-release-hardening/05-02-SUMMARY.md`
</output>
