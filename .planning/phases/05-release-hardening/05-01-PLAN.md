---
phase: 05-release-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/workflows/ci.yml
  - pyproject.toml
  - requirements.txt
  - requirements-dev.txt
  - README.md
autonomous: true
requirements: [QUAL-03]
must_haves:
  truths:
    - "Pull requests run lint, type-check, and test gates automatically"
    - "Quality gates fail deterministically when code violates lint/type/test contracts"
    - "Local developer workflow can run the same checks as CI"
  artifacts:
    - path: ".github/workflows/ci.yml"
      provides: "PR quality gate workflow executing lint, mypy, and pytest"
    - path: "pyproject.toml"
      provides: "Ruff and mypy configuration used by CI and local runs"
    - path: "requirements-dev.txt"
      provides: "Pinned dev dependencies for reproducible quality gate installs"
  key_links:
    - from: ".github/workflows/ci.yml"
      to: "requirements-dev.txt"
      via: "pip install -r requirements-dev.txt"
      pattern: "requirements-dev\.txt"
    - from: ".github/workflows/ci.yml"
      to: "pyproject.toml"
      via: "ruff and mypy command configuration"
      pattern: "ruff check|mypy"
    - from: "README.md"
      to: ".github/workflows/ci.yml"
      via: "documented local parity commands"
      pattern: "ruff|mypy|pytest"
---

<objective>
Establish deterministic quality gates for pull requests and local parity runs.

Purpose: Satisfy QUAL-03 by enforcing lint, type-check, and test execution before merge.
Output: CI workflow, pinned dev dependency lock workflow, and documented parity commands.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/research/STACK.md
@pytest.ini
@src/loan_agents/runtime/service.py

<interfaces>
From src/loan_agents/runtime/service.py:
```python
def run_pipeline(input_payload: dict[str, Any], mode: str) -> dict[str, Any]
```

From pytest.ini:
```ini
[pytest]
testpaths = tests
pythonpath = src
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create reproducible dependency lock workflow and quality tool configuration</name>
  <files>requirements.txt, requirements-dev.txt, pyproject.toml, README.md</files>
  <action>Create pinned runtime and dev dependency files for deterministic installs (per QUAL-03). Add Ruff and mypy settings in pyproject.toml scoped to src and tests. Update README with exact local quality-gate commands that mirror CI: install dev deps, run ruff, run mypy, run pytest. Avoid unpinned dependency specifiers and avoid introducing notebook-only tooling.</action>
  <verify>
    <automated>python -m pip install -r requirements-dev.txt && python -m ruff check src tests && python -m mypy src/loan_agents && python -m pytest -q</automated>
  </verify>
  <done>Pinned dependency files and tool config exist, and local quality commands are documented and executable.</done>
</task>

<task type="auto">
  <name>Task 2: Add pull-request CI workflow enforcing lint, type-check, and tests</name>
  <files>.github/workflows/ci.yml</files>
  <action>Create a GitHub Actions workflow that triggers on pull_request and main pushes, installs Python, installs requirements-dev.txt, and runs ruff, mypy, and pytest in separate explicit steps (per QUAL-03). Keep commands aligned with README parity commands from Task 1 and fail fast on any quality gate failure.</action>
  <verify>
    <automated>python -m pip install -r requirements-dev.txt && python -m ruff check src tests && python -m mypy src/loan_agents && python -m pytest -q</automated>
  </verify>
  <done>Repository contains CI workflow with mandatory lint/type/test gates for pull requests.</done>
</task>

</tasks>

<verification>
- python -m pip install -r requirements-dev.txt
- python -m ruff check src tests
- python -m mypy src/loan_agents
- python -m pytest -q
</verification>

<success_criteria>
- Pull requests and main-branch pushes execute lint, type-check, and test gates automatically.
- Dependency installation for quality tools is deterministic via pinned files.
- Local developers can run the exact CI-equivalent gate commands.
</success_criteria>

<output>
After completion, create `.planning/phases/05-release-hardening/05-01-SUMMARY.md`
</output>
