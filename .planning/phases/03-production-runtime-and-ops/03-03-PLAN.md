---
phase: 03-production-runtime-and-ops
plan: 03
type: execute
wave: 3
depends_on: [03-01, 03-02]
files_modified:
  - src/loan_agents/runtime/metrics.py
  - src/loan_agents/runtime/service.py
  - src/loan_agents/runtime/api.py
  - tests/runtime/test_metrics.py
  - tests/runtime/test_runtime_observability_integration.py
autonomous: true
requirements: [OPER-04, RELI-03]
must_haves:
  truths:
    - "Metrics capture duration, retries, and failure categories per run"
    - "Metrics are emitted for both successful and failed executions"
    - "Mission-report failures and metrics stay consistent for the same run"
  artifacts:
    - path: "src/loan_agents/runtime/metrics.py"
      provides: "Metrics collector for stage durations, retries, and failure categories"
    - path: "tests/runtime/test_metrics.py"
      provides: "Metric emission and aggregation verification"
    - path: "tests/runtime/test_runtime_observability_integration.py"
      provides: "End-to-end observability consistency checks"
  key_links:
    - from: "src/loan_agents/runtime/service.py"
      to: "src/loan_agents/runtime/metrics.py"
      via: "run lifecycle metric emission"
      pattern: "record_stage_duration|record_retry|record_failure"
    - from: "src/loan_agents/runtime/api.py"
      to: "src/loan_agents/runtime/metrics.py"
      via: "run endpoint metrics export"
      pattern: "collect_run_metrics"
    - from: "tests/runtime/test_runtime_observability_integration.py"
      to: "src/loan_agents/runtime/service.py"
      via: "failure envelope and metric parity assertions"
      pattern: "correlation_id"
---

<objective>
Instrument runtime metrics and enforce observability consistency across success/failure paths.

Purpose: Deliver OPER-04 and reinforce RELI-03 mission-report reliability under production telemetry.
Output: Metrics module, service/API instrumentation, and observability integration tests.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-production-runtime-and-ops/03-RESEARCH.md
@.planning/phases/03-production-runtime-and-ops/03-VALIDATION.md
@.planning/phases/03-production-runtime-and-ops/03-01-SUMMARY.md
@.planning/phases/03-production-runtime-and-ops/03-02-SUMMARY.md
@src/loan_agents/runtime/service.py
@src/loan_agents/runtime/api.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add metrics contract tests for duration, retries, and failure categories</name>
  <files>tests/runtime/test_metrics.py, tests/runtime/test_runtime_observability_integration.py</files>
  <read_first>src/loan_agents/runtime/service.py, src/loan_agents/runtime/api.py, tests/runtime/test_failure_mission_report.py, tests/runtime/test_structured_logging.py</read_first>
  <acceptance_criteria>
    - tests/runtime/test_metrics.py validates counters for retries and failure categories.
    - tests/runtime/test_metrics.py validates duration metric is recorded in milliseconds for each stage.
    - integration test asserts same correlation_id appears in mission-report failure and emitted metrics.
  </acceptance_criteria>
  <action>Create metrics-focused tests with concrete expected structures:
- `{"stage_durations_ms": {"document": int}}`
- `{"retry_count": int}`
- `{"failure_categories": {"timeout": int, "rate_limit": int, "provider": int}}`
Add integration assertions for success and failure runs to ensure observability payload consistency.
</action>
  <verify>
    <automated>python -m pytest tests/runtime/test_metrics.py tests/runtime/test_runtime_observability_integration.py -q</automated>
  </verify>
  <done>Metrics and observability tests fail first and fully define OPER-04 behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement runtime metrics module and wire into service/api lifecycle</name>
  <files>src/loan_agents/runtime/metrics.py, src/loan_agents/runtime/service.py, src/loan_agents/runtime/api.py, tests/runtime/test_metrics.py, tests/runtime/test_runtime_observability_integration.py</files>
  <read_first>src/loan_agents/runtime/service.py, src/loan_agents/runtime/api.py, tests/runtime/test_metrics.py, tests/runtime/test_runtime_observability_integration.py</read_first>
  <acceptance_criteria>
    - src/loan_agents/runtime/metrics.py defines functions record_stage_duration, record_retry, record_failure_category, and collect_run_metrics.
    - service and api flows call metrics functions during both success and failure paths.
    - full runtime/foundation test command passes without flaky timing assertions.
  </acceptance_criteria>
  <action>Create `src/loan_agents/runtime/metrics.py` as an in-memory metrics collector keyed by correlation_id. Instrument service and API run handlers to record per-stage duration (ms), retry counts, and failure categories. Include emitted metrics in run responses under `metrics` key for deterministic verification.</action>
  <verify>
    <automated>python -m pytest tests/runtime/test_metrics.py tests/runtime/test_runtime_observability_integration.py tests/runtime/test_failure_mission_report.py tests/foundation/test_service_entrypoint.py -q</automated>
  </verify>
  <done>Metrics are emitted and consistent with mission-report behavior for each run.</done>
</task>

</tasks>

<verification>
- python -m pytest tests/runtime/test_metrics.py tests/runtime/test_runtime_observability_integration.py tests/runtime/test_failure_mission_report.py -q
- python -m pytest tests/runtime tests/foundation -q
</verification>

<success_criteria>
- Metrics for duration, retries, and failures are collected per run.
- Observability data remains consistent with structured failure envelopes.
- OPER-04 is automated-test verified.
</success_criteria>

<output>
After completion, create `.planning/phases/03-production-runtime-and-ops/03-03-SUMMARY.md`
</output>
