---
phase: 03-production-runtime-and-ops
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/loan_agents/runtime/settings.py
  - src/loan_agents/runtime/execution_policy.py
  - src/loan_agents/runtime/errors.py
  - src/loan_agents/runtime/service.py
  - tests/runtime/test_execution_policy.py
  - tests/runtime/test_rate_limiter.py
  - tests/runtime/test_failure_mission_report.py
autonomous: true
requirements: [RELI-01, RELI-02, RELI-03]
must_haves:
  truths:
    - "Runtime enforces bounded retry and exponential backoff with timeout caps"
    - "Rate controls are configurable and prevent unbounded burst execution"
    - "Failures return deterministic mission-report envelopes instead of raw exceptions"
  artifacts:
    - path: "src/loan_agents/runtime/execution_policy.py"
      provides: "Retry/backoff/rate/timeout policy helpers and guarded execution wrapper"
    - path: "src/loan_agents/runtime/service.py"
      provides: "Service dispatch integrated with guarded execution wrapper"
    - path: "tests/runtime/test_failure_mission_report.py"
      provides: "Deterministic failure envelope verification"
  key_links:
    - from: "src/loan_agents/runtime/service.py"
      to: "src/loan_agents/runtime/execution_policy.py"
      via: "guarded execution wrapper"
      pattern: "execute_with_policy"
    - from: "src/loan_agents/runtime/service.py"
      to: "src/loan_agents/runtime/errors.py"
      via: "mission-report failure payload"
      pattern: "build_failure"
    - from: "tests/runtime/test_execution_policy.py"
      to: "src/loan_agents/runtime/execution_policy.py"
      via: "retry/backoff assertions"
      pattern: "RuntimeExecutionPolicy"
---

<objective>
Add production-safe runtime guardrails (retry, rate, timeout) and deterministic mission-report failures.

Purpose: Deliver RELI-01/RELI-02/RELI-03 baseline needed before operational endpoint wiring.
Output: Execution policy module, guarded service execution path, and runtime reliability tests.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-production-runtime-and-ops/03-RESEARCH.md
@.planning/phases/03-production-runtime-and-ops/03-VALIDATION.md
@src/loan_agents/runtime/service.py
@src/loan_agents/runtime/settings.py
@src/loan_agents/runtime/errors.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create runtime reliability test suite and expected mission-report contracts</name>
  <files>tests/runtime/test_execution_policy.py, tests/runtime/test_rate_limiter.py, tests/runtime/test_failure_mission_report.py</files>
  <read_first>src/loan_agents/runtime/service.py, src/loan_agents/runtime/settings.py, src/loan_agents/runtime/errors.py, tests/foundation/test_service_entrypoint.py</read_first>
  <acceptance_criteria>
    - tests/runtime/test_execution_policy.py contains test names with retry, backoff, and timeout assertions.
    - tests/runtime/test_rate_limiter.py contains assertions that more than configured requests are blocked or delayed.
    - tests/runtime/test_failure_mission_report.py asserts output keys include correlation_id, failure_category, retry_count, and stage.
  </acceptance_criteria>
  <action>Create new runtime test files that define expected behavior for bounded retry (`MAX_RETRIES`), exponential backoff caps (`BACKOFF_MIN_SECONDS`, `BACKOFF_MAX_SECONDS`), timeout cap (`RUN_TIMEOUT_SECONDS`), and rate control (`REQUESTS_PER_MINUTE`). Encode mission-report failure expectations with concrete keys `error.code`, `error.message`, `error.failure_category`, `error.retry_count`, and top-level `correlation_id`.</action>
  <verify>
    <automated>python -m pytest tests/runtime/test_execution_policy.py tests/runtime/test_rate_limiter.py tests/runtime/test_failure_mission_report.py -q</automated>
  </verify>
  <done>Runtime reliability tests exist and fail before policy implementation (RED).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement execution policy module and configurable runtime controls</name>
  <files>src/loan_agents/runtime/settings.py, src/loan_agents/runtime/execution_policy.py</files>
  <read_first>src/loan_agents/runtime/settings.py, .planning/phases/03-production-runtime-and-ops/03-RESEARCH.md, tests/runtime/test_execution_policy.py, tests/runtime/test_rate_limiter.py</read_first>
  <acceptance_criteria>
    - src/loan_agents/runtime/settings.py defines settings fields MAX_RETRIES, BACKOFF_MULTIPLIER, BACKOFF_MIN_SECONDS, BACKOFF_MAX_SECONDS, REQUESTS_PER_MINUTE, and RUN_TIMEOUT_SECONDS.
    - src/loan_agents/runtime/execution_policy.py defines RuntimeExecutionPolicy dataclass and execute_with_policy function.
    - Retry logic enforces a hard upper bound equal to MAX_RETRIES and exponential backoff clamped by BACKOFF_MAX_SECONDS.
  </acceptance_criteria>
  <action>Extend RuntimeSettings with integer/float policy fields sourced from environment variables. Create `src/loan_agents/runtime/execution_policy.py` containing `RuntimeExecutionPolicy` and `execute_with_policy(callable_fn, *, policy, correlation_id, stage)` that enforces bounded retries, capped exponential backoff, timeout checks, and per-minute request gating without unbounded loops.</action>
  <verify>
    <automated>python -m pytest tests/runtime/test_execution_policy.py tests/runtime/test_rate_limiter.py -q</automated>
  </verify>
  <done>Policy implementation satisfies retry/backoff/rate/timeout expectations deterministically.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Integrate guarded execution into service with mission-report failure envelope</name>
  <files>src/loan_agents/runtime/service.py, src/loan_agents/runtime/errors.py, tests/runtime/test_failure_mission_report.py</files>
  <read_first>src/loan_agents/runtime/service.py, src/loan_agents/runtime/errors.py, src/loan_agents/runtime/execution_policy.py, tests/runtime/test_failure_mission_report.py</read_first>
  <acceptance_criteria>
    - src/loan_agents/runtime/service.py calls execute_with_policy for adapter invocation.
    - src/loan_agents/runtime/errors.py build_failure output includes failure_category and retry_count fields in error payload.
    - mission-report tests verify deterministic failure category values for timeout, rate_limit, and provider errors.
  </acceptance_criteria>
  <action>Update `run_pipeline` to create correlation-aware guarded calls through `execute_with_policy` and map raised policy exceptions into `build_failure` payloads using concrete `failure_category` values (`timeout`, `rate_limit`, `provider`, `config`, `invalid_mode`) and integer `retry_count`. Preserve stable response keys `status`, `decision`, `mode`, `stages`, and `error`.</action>
  <verify>
    <automated>python -m pytest tests/runtime/test_failure_mission_report.py tests/foundation/test_service_entrypoint.py tests/foundation/test_mode_contract.py -q</automated>
  </verify>
  <done>Service returns structured mission-report failures with policy metadata instead of raw exceptions.</done>
</task>

</tasks>

<verification>
- python -m pytest tests/runtime/test_execution_policy.py tests/runtime/test_rate_limiter.py tests/runtime/test_failure_mission_report.py -q
- python -m pytest tests/foundation/test_service_entrypoint.py tests/foundation/test_mode_contract.py -q
</verification>

<success_criteria>
- Runtime guardrails are configurable and enforced at execution time.
- Failure responses include deterministic mission-report metadata.
- RELI-01, RELI-02, and RELI-03 are covered by automated tests.
</success_criteria>

<output>
After completion, create `.planning/phases/03-production-runtime-and-ops/03-01-SUMMARY.md`
</output>
