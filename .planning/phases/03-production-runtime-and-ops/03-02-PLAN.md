---
phase: 03-production-runtime-and-ops
plan: 02
type: execute
wave: 2
depends_on: [03-01]
files_modified:
  - src/loan_agents/runtime/api.py
  - src/loan_agents/runtime/logging.py
  - src/loan_agents/runtime/service.py
  - src/loan_agents/runtime/redaction.py
  - tests/runtime/test_runtime_api_endpoints.py
  - tests/runtime/test_structured_logging.py
autonomous: true
requirements: [OPER-01, OPER-02, OPER-03, SECU-03]
must_haves:
  truths:
    - "Runtime exposes health and readiness endpoint handlers"
    - "Run endpoint accepts typed input and returns standardized payload"
    - "Logs include correlation ID and stage telemetry while masking secrets"
  artifacts:
    - path: "src/loan_agents/runtime/api.py"
      provides: "health/readiness/run endpoint handler functions"
    - path: "src/loan_agents/runtime/logging.py"
      provides: "Structured logging formatter with correlation and stage fields"
    - path: "tests/runtime/test_structured_logging.py"
      provides: "Redaction and correlation logging assertions"
  key_links:
    - from: "src/loan_agents/runtime/api.py"
      to: "src/loan_agents/runtime/service.py"
      via: "run endpoint dispatch"
      pattern: "run_pipeline"
    - from: "src/loan_agents/runtime/logging.py"
      to: "src/loan_agents/runtime/redaction.py"
      via: "secret masking"
      pattern: "redact_secret"
    - from: "tests/runtime/test_runtime_api_endpoints.py"
      to: "src/loan_agents/runtime/api.py"
      via: "endpoint contract assertions"
      pattern: "health|readiness|run"
---

<objective>
Add operational endpoint surface and structured telemetry logging with secret redaction.

Purpose: Deliver OPER-01/OPER-02/OPER-03 and complete SECU-03 runtime behavior.
Output: Runtime endpoint handlers, structured logging utility, and endpoint/logging tests.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-production-runtime-and-ops/03-RESEARCH.md
@src/loan_agents/runtime/service.py
@src/loan_agents/runtime/redaction.py
@src/loan_agents/domain/contracts.py
@.planning/phases/03-production-runtime-and-ops/03-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define endpoint and logging behavior tests for runtime operations</name>
  <files>tests/runtime/test_runtime_api_endpoints.py, tests/runtime/test_structured_logging.py</files>
  <read_first>src/loan_agents/runtime/service.py, src/loan_agents/runtime/redaction.py, tests/foundation/test_service_entrypoint.py</read_first>
  <acceptance_criteria>
    - tests/runtime/test_runtime_api_endpoints.py contains tests for health, readiness, and run handlers.
    - tests/runtime/test_structured_logging.py asserts presence of correlation_id, stage, mode, status keys in log payload.
    - tests/runtime/test_structured_logging.py asserts secret tokens are replaced with ***REDACTED***.
  </acceptance_criteria>
  <action>Create tests that assert `health()` returns status `ok`, `readiness()` reflects configuration readiness, and `run()` accepts payload fields `applicant_id`, `document_id`, and `mode`. Add log tests that verify JSON-serializable structured event payloads include `correlation_id`, `stage`, `duration_ms`, and redact sensitive values (`LLM_API_KEY`, bearer tokens, any key named secret/password/token`).</action>
  <verify>
    <automated>python -m pytest tests/runtime/test_runtime_api_endpoints.py tests/runtime/test_structured_logging.py -q</automated>
  </verify>
  <done>Operational endpoint and logging expectations are codified and failing first (RED).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement runtime API handlers for health, readiness, and typed run</name>
  <files>src/loan_agents/runtime/api.py, src/loan_agents/runtime/service.py, tests/runtime/test_runtime_api_endpoints.py</files>
  <read_first>src/loan_agents/runtime/service.py, src/loan_agents/domain/contracts.py, tests/runtime/test_runtime_api_endpoints.py</read_first>
  <acceptance_criteria>
    - src/loan_agents/runtime/api.py defines functions health(), readiness(), and run().
    - run() validates and forwards typed payload through PipelineInput.from_dict before service dispatch.
    - readiness() returns failed status when required configuration is missing.
  </acceptance_criteria>
  <action>Create `src/loan_agents/runtime/api.py` with endpoint-style handlers:
- `health() -> {"status": "ok", "service": "loan-agents"}`
- `readiness() -> {"status": "ready"|"not_ready", "checks": {...}}`
- `run(request: dict) -> standardized pipeline response`
Use explicit request shape keys `applicant_id`, `document_id`, `mode`, `correlation_id` and return normalized payload from `run_pipeline`.
</action>
  <verify>
    <automated>python -m pytest tests/runtime/test_runtime_api_endpoints.py tests/foundation/test_service_entrypoint.py -q</automated>
  </verify>
  <done>Operational handlers provide typed execution and deployment-check readiness responses.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implement structured logging with correlation IDs and redaction enforcement</name>
  <files>src/loan_agents/runtime/logging.py, src/loan_agents/runtime/redaction.py, src/loan_agents/runtime/service.py, tests/runtime/test_structured_logging.py</files>
  <read_first>src/loan_agents/runtime/redaction.py, src/loan_agents/runtime/service.py, tests/runtime/test_structured_logging.py</read_first>
  <acceptance_criteria>
    - src/loan_agents/runtime/logging.py exposes log_stage_event() and log_failure_event() helpers.
    - log payload contains keys timestamp, correlation_id, stage, mode, status, duration_ms.
    - tests verify no raw secret values appear in serialized logs.
  </acceptance_criteria>
  <action>Create structured logging helpers that emit dictionary payloads with required telemetry fields and sanitized metadata. Update service flow to emit stage start/success/failure events using correlation IDs from request context. Extend redaction utilities to sanitize dictionaries by key name (`secret`, `token`, `password`, `api_key`) and value shape before logging.</action>
  <verify>
    <automated>python -m pytest tests/runtime/test_structured_logging.py tests/runtime/test_runtime_api_endpoints.py -q</automated>
  </verify>
  <done>Runtime telemetry logs are correlation-aware and secret-safe per SECU-03.</done>
</task>

</tasks>

<verification>
- python -m pytest tests/runtime/test_runtime_api_endpoints.py tests/runtime/test_structured_logging.py -q
- python -m pytest tests/foundation/test_service_entrypoint.py -q
</verification>

<success_criteria>
- Health and readiness handlers are callable and deterministic.
- Typed run handler routes through shared runtime service contract.
- Structured logs include correlation/stage telemetry with redacted sensitive data.
</success_criteria>

<output>
After completion, create `.planning/phases/03-production-runtime-and-ops/03-02-SUMMARY.md`
</output>
