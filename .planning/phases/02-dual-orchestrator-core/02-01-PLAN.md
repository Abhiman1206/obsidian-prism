---
phase: 02-dual-orchestrator-core
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/loan_agents/orchestration/normalized.py
  - src/loan_agents/orchestration/crewai_adapter.py
  - src/loan_agents/orchestration/langgraph_adapter.py
  - src/loan_agents/orchestration/__init__.py
  - src/loan_agents/domain/contracts.py
  - tests/foundation/test_orchestrator_output_contract.py
autonomous: true
requirements: [ARCH-03]
must_haves:
  truths:
    - "Both orchestration strategies return the same normalized output contract"
    - "Adapter outputs differ only by declared mode for the same deterministic document"
    - "Unknown document inputs return deterministic structured failures"
  artifacts:
    - path: "src/loan_agents/orchestration/normalized.py"
      provides: "Shared result normalization helpers used by both adapters"
    - path: "src/loan_agents/orchestration/crewai_adapter.py"
      provides: "CrewAI adapter delegating to shared normalization"
    - path: "src/loan_agents/orchestration/langgraph_adapter.py"
      provides: "LangGraph adapter delegating to shared normalization"
    - path: "tests/foundation/test_orchestrator_output_contract.py"
      provides: "Parity and contract assertions for adapter outputs"
  key_links:
    - from: "src/loan_agents/orchestration/crewai_adapter.py"
      to: "src/loan_agents/orchestration/normalized.py"
      via: "shared success/failure builders"
      pattern: "build_success_result|build_adapter_failure"
    - from: "src/loan_agents/orchestration/langgraph_adapter.py"
      to: "src/loan_agents/orchestration/normalized.py"
      via: "shared success/failure builders"
      pattern: "build_success_result|build_adapter_failure"
    - from: "tests/foundation/test_orchestrator_output_contract.py"
      to: "src/loan_agents/orchestration/*.py"
      via: "adapter parity assertions"
      pattern: "run_crewai_pipeline|run_langgraph_pipeline"
---

<objective>
Normalize framework adapter outputs behind shared domain contracts so both orchestrators remain contract-compatible.

Purpose: Satisfy ARCH-03 by enforcing one shared output schema regardless of selected framework.
Output: Shared normalization utility, refactored adapters, and parity-focused adapter contract tests.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/loan_agents/domain/contracts.py
@src/loan_agents/orchestration/crewai_adapter.py
@src/loan_agents/orchestration/langgraph_adapter.py
@tests/foundation/test_contracts.py
@tests/foundation/test_mode_contract.py

<interfaces>
From src/loan_agents/domain/contracts.py:
```python
@dataclass(frozen=True)
class PipelineResult:
    status: str
    decision: str
    mode: str
    stages: dict[str, Any]
    error: dict[str, Any] | None = None

    def to_dict(self) -> dict[str, Any]:
        return {
            "status": self.status,
            "decision": self.decision,
            "mode": self.mode,
            "stages": self.stages,
            "error": self.error,
        }
```

From src/loan_agents/orchestration/crewai_adapter.py and src/loan_agents/orchestration/langgraph_adapter.py:
```python
def run_crewai_pipeline(input_payload: dict[str, Any]) -> dict[str, Any]: ...
def run_langgraph_pipeline(input_payload: dict[str, Any]) -> dict[str, Any]: ...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add adapter parity contract tests for shared output schema</name>
  <files>tests/foundation/test_orchestrator_output_contract.py</files>
  <behavior>
    - Test 1: Valid sample input returns required keys for both adapters.
    - Test 2: The same sample input yields equal decision and stage payload in both adapters.
    - Test 3: Unknown document_id returns deterministic failure shape for both adapters.
  </behavior>
  <action>Create `tests/foundation/test_orchestrator_output_contract.py` with direct adapter-level tests that assert output key set (`status`, `decision`, `mode`, `stages`, `error`) and deterministic parity between crewai/langgraph for matching inputs.</action>
  <verify>
    <automated>python -m pytest tests/foundation/test_orchestrator_output_contract.py -q</automated>
  </verify>
  <done>Adapter parity tests fail first (RED) and clearly encode ARCH-03 contract expectations.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Refactor adapters to use shared normalization helpers</name>
  <files>src/loan_agents/orchestration/normalized.py, src/loan_agents/orchestration/crewai_adapter.py, src/loan_agents/orchestration/langgraph_adapter.py, src/loan_agents/orchestration/__init__.py, src/loan_agents/domain/contracts.py</files>
  <behavior>
    - Test 1: Both adapters build success payloads through the same helper path.
    - Test 2: Adapter failures become normalized envelopes without leaking implementation-specific shape.
  </behavior>
  <action>Create `src/loan_agents/orchestration/normalized.py` with helper functions that construct `PipelineResult` dictionaries for success/failure. Update both adapters to delegate all result shaping to this module (per ARCH-03) while keeping framework-specific execution code isolated in the adapter files. Export any new helper used externally via orchestration package init only if required.</action>
  <verify>
    <automated>python -m pytest tests/foundation/test_orchestrator_output_contract.py tests/foundation/test_mode_contract.py -q</automated>
  </verify>
  <done>Both adapters pass parity tests and produce normalized output via shared contracts.</done>
</task>

</tasks>

<verification>
- `python -m pytest tests/foundation/test_orchestrator_output_contract.py tests/foundation/test_mode_contract.py -q` passes.
- Adapter modules do not duplicate contract serialization logic.
- Success and failure output keys are identical across both modes.
</verification>

<success_criteria>
- Shared domain contract is the single source of truth for adapter outputs.
- CrewAI and LangGraph adapter outputs are structurally and semantically aligned.
</success_criteria>

<output>
After completion, create `.planning/phases/02-dual-orchestrator-core/02-01-SUMMARY.md`
</output>
