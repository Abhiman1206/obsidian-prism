---
phase: 02-dual-orchestrator-core
plan: 02
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - src/loan_agents/runtime/service.py
  - tests/foundation/test_service_entrypoint.py
  - tests/foundation/test_dual_mode_samples.py
  - README.md
autonomous: true
requirements: [ARCH-02, ARCH-03]
must_haves:
  truths:
    - "A single input schema can run either CrewAI or LangGraph without payload changes"
    - "Service output remains normalized for both orchestration modes"
    - "Chapter sample-style documents execute successfully in both modes"
  artifacts:
    - path: "src/loan_agents/runtime/service.py"
      provides: "Single typed service entrypoint dispatching both orchestration modes"
    - path: "tests/foundation/test_dual_mode_samples.py"
      provides: "Sample matrix proving one input contract executes both modes"
    - path: "tests/foundation/test_service_entrypoint.py"
      provides: "Entrypoint-mode and failure contract coverage"
  key_links:
    - from: "src/loan_agents/runtime/service.py"
      to: "src/loan_agents/domain/contracts.py"
      via: "PipelineInput.from_dict normalization before dispatch"
      pattern: "PipelineInput\.from_dict"
    - from: "src/loan_agents/runtime/service.py"
      to: "src/loan_agents/orchestration/*.py"
      via: "mode dispatch over single normalized payload"
      pattern: "run_crewai_pipeline|run_langgraph_pipeline"
    - from: "tests/foundation/test_dual_mode_samples.py"
      to: "src/loan_agents/runtime/service.py"
      via: "same payload across both modes"
      pattern: "run_pipeline"
---

<objective>
Complete dual-orchestrator execution parity by enforcing one service input contract and validating sample scenarios across both orchestration modes.

Purpose: Satisfy ARCH-02 while preserving ARCH-03 guarantees from plan 02-01.
Output: Hardened service dispatch path, mode-matrix tests, and updated runtime documentation.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-dual-orchestrator-core/02-01-PLAN.md
@src/loan_agents/runtime/service.py
@src/loan_agents/domain/contracts.py
@src/loan_agents/orchestration/crewai_adapter.py
@src/loan_agents/orchestration/langgraph_adapter.py
@tests/foundation/test_service_entrypoint.py
@src/loan_agents/document/mock_data.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add dual-mode sample matrix tests using one input schema</name>
  <files>tests/foundation/test_dual_mode_samples.py, tests/foundation/test_service_entrypoint.py</files>
  <behavior>
    - Test 1: Identical payload executes successfully in both modes for valid sample document.
    - Test 2: Risky sample document yields deterministic decision parity across modes.
    - Test 3: Invalid/unknown sample document returns normalized failure envelope with selected mode preserved.
  </behavior>
  <action>Create `tests/foundation/test_dual_mode_samples.py` to run chapter sample-style payloads through `run_pipeline` for both modes without changing input structure. Extend `tests/foundation/test_service_entrypoint.py` only where needed to keep entrypoint contract coverage aligned.</action>
  <verify>
    <automated>python -m pytest tests/foundation/test_dual_mode_samples.py tests/foundation/test_service_entrypoint.py -q</automated>
  </verify>
  <done>Tests encode ARCH-02 expectations and fail before service changes (RED).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Harden service dispatch around shared typed input normalization</name>
  <files>src/loan_agents/runtime/service.py</files>
  <behavior>
    - Test 1: Service normalizes input through `PipelineInput` once and reuses normalized payload for both adapters.
    - Test 2: Invalid shape or adapter/runtime failures return deterministic structured envelope.
  </behavior>
  <action>Update `run_pipeline` to normalize request data once through `PipelineInput.from_dict`, pass a normalized payload into both adapter calls, and preserve selected mode in every failure path. Keep explicit mode validation and avoid fallback mode switching (per established phase-1 decision).</action>
  <verify>
    <automated>python -m pytest tests/foundation/test_dual_mode_samples.py tests/foundation/test_service_entrypoint.py tests/foundation/test_mode_contract.py -q</automated>
  </verify>
  <done>A single input schema drives both orchestrators and returns normalized outputs consistently.</done>
</task>

<task type="auto">
  <name>Task 3: Document dual-orchestrator usage and run full foundation suite</name>
  <files>README.md</files>
  <action>Expand README runtime section with a concise dual-mode usage example showing one input payload executed with `crewai` and `langgraph`, and document normalized response guarantees.</action>
  <verify>
    <automated>python -m pytest tests/foundation -q</automated>
  </verify>
  <done>Documentation reflects the implemented ARCH-02/ARCH-03 behavior and full foundation suite is green.</done>
</task>

</tasks>

<verification>
- `python -m pytest tests/foundation -q` passes.
- Same payload shape executes both `crewai` and `langgraph` paths.
- Runtime documentation clearly states single-input, dual-mode behavior.
</verification>

<success_criteria>
- Shared service contract drives both orchestrators without caller-specific payload variants.
- Normalized outputs remain contract-compatible across frameworks for sample scenarios.
- Phase 2 requirements ARCH-02 and ARCH-03 are demonstrably test-covered.
</success_criteria>

<output>
After completion, create `.planning/phases/02-dual-orchestrator-core/02-02-SUMMARY.md`
</output>
