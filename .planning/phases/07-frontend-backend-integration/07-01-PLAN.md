---
phase: 07-frontend-backend-integration
plan: 01
type: execute
wave: 2
depends_on: [06-01]
files_modified:
  - frontend/src/lib/api.ts
  - frontend/src/App.tsx
  - frontend/src/config/env.ts
  - src/loan_agents/runtime/settings.py
  - src/loan_agents/runtime/asgi.py
  - src/loan_agents/runtime/errors.py
  - tests/runtime/test_runtime_api_endpoints.py
  - tests/runtime/test_runtime_observability_integration.py
  - frontend/tests/
  - README.md
autonomous: true
requirements: [INT-01, INT-02]
must_haves:
  truths:
    - "Frontend submits typed requests to backend and renders normalized response payloads"
    - "Backend accepts configured frontend origins through explicit environment-based CORS policy"
    - "End-to-end flow verifies success and structured failure handling"
  artifacts:
    - path: "frontend/src/lib/api.ts"
      provides: "Runtime-backed API client with timeout, structured error-envelope mapping, and payload typing"
    - path: "src/loan_agents/runtime/settings.py"
      provides: "CORS origin configuration and validation"
    - path: "src/loan_agents/runtime/asgi.py"
      provides: "ASGI middleware wiring for CORS aligned with frontend integration needs"
  key_links:
    - from: "frontend/src/lib/api.ts"
      to: "src/loan_agents/runtime/api.py"
      via: "POST /run and health/readiness endpoints"
      pattern: "run|health|readiness"
    - from: "src/loan_agents/runtime/settings.py"
      to: "src/loan_agents/runtime/asgi.py"
      via: "allowed origins configuration"
      pattern: "origin|cors"
---

<objective>
Connect frontend workflows to the backend runtime service with safe cross-origin behavior.

Purpose: Satisfy INT-01 and INT-02 by wiring frontend API calls to backend endpoints and adding explicit CORS configuration controls.
Output: Integrated API client flow, backend CORS settings, and cross-boundary integration tests/smoke checks.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@src/loan_agents/runtime/api.py
@src/loan_agents/runtime/asgi.py
@src/loan_agents/runtime/settings.py
@.planning/phases/06-frontend-application/06-01-PLAN.md

<interfaces>
From src/loan_agents/runtime/api.py:
```python
def health() -> dict[str, Any]
def readiness() -> dict[str, Any]
def run(request: dict[str, Any]) -> dict[str, Any]
```

Frontend API interface:
```ts
type RunResponse = {
  status: string;
  decision: string;
  mode: string;
  stages: Record<string, unknown>;
  error?: {
    code: string;
    message: string;
    failure_category?: string;
    retry_count?: number;
    stage?: string;
  } | null;
};
```
</interfaces>
</context>

<requirement_mapping>
- INT-01 -> Task 1, Task 3
- INT-02 -> Task 2, Task 3
</requirement_mapping>

<tasks>

<task type="auto">
  <name>Task 1: Wire frontend API client to backend runtime endpoints</name>
  <files>frontend/src/lib/api.ts, frontend/src/config/env.ts, frontend/src/App.tsx</files>
  <action>Replace mock-only client behavior with environment-configured backend base URL integration for health/readiness/run endpoints. Ensure request/response typing matches the runtime envelope (status/decision/mode/stages/error), timeout handling, and deterministic UI rendering for structured failures from the error field.</action>
  <verify>
    <automated>npm --prefix frontend run test -- --run api && npm --prefix frontend run build</automated>
  </verify>
  <done>Frontend executes real backend calls and consistently renders success/failure states from the normalized runtime envelope.</done>
</task>

<task type="auto">
  <name>Task 2: Add backend CORS configuration and enforcement</name>
  <files>src/loan_agents/runtime/settings.py, src/loan_agents/runtime/asgi.py, tests/runtime/test_runtime_api_endpoints.py</files>
  <action>Add explicit allowed-origin configuration in runtime settings and apply it in the ASGI app middleware layer. Ensure browser-visible CORS behavior is environment-controlled, rejects unexpected origins safely, and is documented for local/staging/prod scenarios.</action>
  <verify>
    <automated>python -m pytest tests/runtime/test_runtime_api_endpoints.py -q</automated>
  </verify>
  <done>Backend cross-origin behavior is environment-controlled and covered by tests.</done>
</task>

<task type="auto">
  <name>Task 3: Add end-to-end integration checks and documentation</name>
  <files>frontend/tests/, tests/runtime/test_runtime_observability_integration.py, README.md</files>
  <action>Add integration tests and smoke instructions that validate end-to-end request flow from frontend to backend for both success and structured failure outcomes using the runtime error envelope. Update README with local connected run sequence and troubleshooting for CORS/config issues.</action>
  <verify>
    <automated>npm --prefix frontend run test -- --run integration && python -m pytest tests/runtime/test_runtime_observability_integration.py -q</automated>
  </verify>
  <done>Connected flow is test-backed, documented, and reproducible in local/staging environments.</done>
</task>

</tasks>

<verification>
- npm --prefix frontend run lint
- npm --prefix frontend run test -- --run
- npm --prefix frontend run build
- python -m pytest tests/runtime/test_runtime_api_endpoints.py -q
- python -m pytest tests/runtime/test_runtime_observability_integration.py -q
</verification>

<risks>
- Misconfigured CORS can block legitimate traffic or unintentionally broaden allowed origins.
- Backend response drift can break typed frontend rendering logic.
- Local network differences (ports/proxies) can hide integration defects until staging.
</risks>

<assumptions>
- Phase 6 frontend skeleton and Phase 3 runtime endpoints remain available and stable.
- Backend API contract remains normalized and structured failures are represented under the error envelope.
- Environment variable management is available for frontend and backend in each environment.
</assumptions>

<rollback>
- Revert to frontend mock API mode while preserving UI components if backend integration destabilizes releases.
- Disable new CORS strictness temporarily via safe default env configuration while hotfixing origin lists.
- Keep integration changes in isolated commits across frontend and backend to allow targeted rollback.
</rollback>

<success_criteria>
- Frontend can execute the backend run flow and render normalized outputs and structured error-envelope failures.
- Backend CORS policy is explicit, test-backed, and environment-configurable.
- End-to-end integration flow is documented and validated by automated checks.
</success_criteria>

<output>
After completion, create `.planning/phases/07-frontend-backend-integration/07-01-SUMMARY.md`
</output>
