---
phase: 06-frontend-application
plan: 01
type: execute
wave: 1
depends_on: [05-02]
files_modified:
  - frontend/package.json
  - frontend/package-lock.json
  - frontend/tsconfig.json
  - frontend/vite.config.ts
  - frontend/src/main.tsx
  - frontend/src/App.tsx
  - frontend/src/routes/
  - frontend/src/components/
  - frontend/src/lib/api.ts
  - frontend/src/lib/validation.ts
  - frontend/src/styles/
  - frontend/tests/
  - README.md
autonomous: true
requirements: [FE-01, FE-02]
must_haves:
  truths:
    - "Frontend application captures required runtime contract fields and renders normalized decision outcomes"
    - "Client-side validation mirrors backend PipelineInput contract and allowed mode values before request submission"
    - "Frontend has deterministic lint, test, and build verification commands"
  artifacts:
    - path: "frontend/src/lib/validation.ts"
      provides: "Typed validation rules for input payloads aligned with backend contract"
    - path: "frontend/src/lib/api.ts"
      provides: "Runtime API client abstraction for later backend integration"
    - path: "frontend/package.json"
      provides: "Frontend toolchain scripts for lint, test, and build"
  key_links:
    - from: "frontend/src/lib/validation.ts"
      to: "src/loan_agents/domain/contracts.py"
      via: "field-level input compatibility"
      pattern: "PipelineInput|applicant_id|document_id|mode"
    - from: "frontend/src/App.tsx"
      to: "frontend/src/lib/api.ts"
      via: "submit and result rendering workflow"
      pattern: "submit|result|error"
---

<objective>
Create the standalone frontend application baseline for loan workflow user interactions.

Purpose: Satisfy FE-01 and FE-02 through a typed React app that captures loan input data, validates it client-side, and renders deterministic progress/result/error states.
Output: Frontend scaffold, contract-aligned validation/types, deterministic UI state flow, and CI-ready lint/typecheck/test/build commands.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@src/loan_agents/domain/contracts.py
@README.md

<interfaces>
From src/loan_agents/domain/contracts.py:
```python
@dataclass(frozen=True)
class PipelineInput:
    applicant_id: str
    document_id: str
    mode: str
```

Planned frontend interface:
```ts
type PipelineInput = {
  applicant_id: string;
  document_id: string;
  mode: "crewai" | "langgraph";
};
```
</interfaces>
</context>

<requirement_mapping>
- FE-01 -> Tasks 1, 3, 4, 5
- FE-02 -> Tasks 2, 3, 5
</requirement_mapping>

<tasks>

<task type="auto">
  <name>Task 1: Bootstrap frontend toolchain and app shell</name>
  <files>frontend/package.json, frontend/package-lock.json, frontend/tsconfig.json, frontend/vite.config.ts, frontend/src/main.tsx, frontend/src/App.tsx, frontend/src/styles/</files>
  <action>Create a React + TypeScript Vite app under frontend/ with pinned dependencies and deterministic scripts: lint, typecheck, test, and build. Add a minimal app shell layout with clear sections for Intake, Progress, and Decision Summary views.</action>
  <verify>
    <automated>npm --prefix frontend ci && npm --prefix frontend run lint && npm --prefix frontend run typecheck && npm --prefix frontend run build</automated>
  </verify>
  <done>Frontend scaffold compiles and quality scripts run consistently across local and CI environments.</done>
</task>

<task type="auto">
  <name>Task 2: Define contract-aligned frontend types and validation schema</name>
  <files>frontend/src/lib/validation.ts, frontend/src/lib/types.ts, frontend/tests/</files>
  <action>Implement TypeScript interfaces mirroring PipelineInput and create validation helpers enforcing required-field presence, trimmed non-empty string values, and mode enum checks (crewai|langgraph). Keep validation scope strictly aligned to backend contract shape (no extra numeric/domain constraints). Add unit tests for valid payloads and each invalid rule branch.</action>
  <verify>
    <automated>npm --prefix frontend run test -- --run validation types</automated>
  </verify>
  <done>Validation layer reliably accepts valid contract-shaped input and rejects invalid payloads with deterministic errors.</done>
</task>

<task type="auto">
  <name>Task 3: Build intake form components and submission guardrails</name>
  <files>frontend/src/components/, frontend/src/App.tsx, frontend/tests/</files>
  <action>Implement reusable form controls for applicant_id, document_id, and mode with field-level error display. Wire submit flow so invalid inputs are blocked, valid inputs are transformed into PipelineInput-compatible payloads, and submit button state is deterministic.</action>
  <verify>
    <automated>npm --prefix frontend run test -- --run form app</automated>
  </verify>
  <done>Users can enter loan inputs, receive actionable validation feedback, and only submit contract-valid payloads.</done>
</task>

<task type="auto">
  <name>Task 4: Implement progress, success, and structured failure rendering</name>
  <files>frontend/src/lib/api.ts, frontend/src/components/, frontend/src/App.tsx, frontend/tests/</files>
  <action>Add UI states for idle/submitting/success/failure and render normalized outcome fields plus mission-report style failures. Define explicit FE-01 progress acceptance for this phase: a deterministic request lifecycle progress indicator (idle -> submitting -> success|failure), with no stage-level progress dependency. Use an API abstraction with local mock mode to keep Phase 6 standalone while preserving integration seam for Phase 7.</action>
  <verify>
    <automated>npm --prefix frontend run test -- --run states result error</automated>
  </verify>
  <done>Frontend visibly communicates request progress and deterministic success/failure outcomes without backend coupling.</done>
</task>

<task type="auto">
  <name>Task 5: Document and harden CI-ready frontend quality commands</name>
  <files>README.md, frontend/package.json, frontend/package-lock.json, frontend/tests/</files>
  <action>Generate and commit package-lock.json and document frontend setup/commands in README. Ensure scripts are stable for CI usage: npm ci, lint, typecheck, test, and build. Add at least one end-to-end component-flow test covering valid submit path and invalid-field handling.</action>
  <verify>
    <automated>npm --prefix frontend ci && npm --prefix frontend run lint && npm --prefix frontend run typecheck && npm --prefix frontend run test -- --run && npm --prefix frontend run build</automated>
  </verify>
  <done>Project contains a reproducible frontend verification path suitable for pull-request quality checks.</done>
</task>

</tasks>

<verification>
- npm --prefix frontend ci
- npm --prefix frontend run lint
- npm --prefix frontend run typecheck
- npm --prefix frontend run test -- --run
- npm --prefix frontend run build
- npm --prefix frontend run dev (manual smoke: load form, trigger validation, submit mock success, submit mock failure)
</verification>

<verification_strategy>
- Unit tests: validation helpers, PipelineInput payload transformation, and API state reducers.
- Component tests: intake form interaction, validation message behavior, and submit-state transitions.
- Runtime checks: local dev server smoke path proving explicit request lifecycle progress (idle -> submitting -> success|failure), plus structured failure rendering.
- Quality gates: lint + typecheck + tests + build must all pass before phase summary sign-off.
</verification_strategy>

<risks>
- Contract drift between frontend TypeScript types and backend Python contracts can break later integration.
- Introducing a new JavaScript toolchain may create CI/runtime differences if versions are not pinned.
- UI complexity can expand scope if non-essential pages are added in this phase.
- Team expectations may assume stage-level progress while this phase only commits to deterministic request-lifecycle progress.
</risks>

<assumptions>
- Backend contract fields in `src/loan_agents/domain/contracts.py` are stable enough for Phase 6 implementation.
- Allowed runtime modes remain `crewai` and `langgraph` for frontend enum validation in this phase.
- Node.js and npm are available in CI and developer environments.
- Initial frontend can ship without authentication and with mock API mode for local progression.
</assumptions>

<rollback>
- Revert frontend scaffold commit(s) and restore previous README if build pipeline or repository standards are disrupted.
- Keep frontend isolated under `frontend/` so rollback is path-contained.
- If validation schema causes regressions, fall back to exact backend shape checks only (required + non-empty + mode enum) and defer any extra UX constraints to later phases.
</rollback>

<success_criteria>
- Frontend collects all required PipelineInput fields (applicant_id, document_id, mode) and displays decision output states.
- Typed validation is enforced before submission and covered by tests.
- FE-01 progress requirement is satisfied via deterministic request lifecycle indicator (idle -> submitting -> success|failure).
- Frontend lint/test/build commands run deterministically and are documented.
</success_criteria>

<output>
After completion, create `.planning/phases/06-frontend-application/06-01-SUMMARY.md`
</output>
