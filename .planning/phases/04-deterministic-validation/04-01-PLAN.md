---
phase: 04-deterministic-validation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/loan_agents/orchestration/normalized.py
  - src/loan_agents/orchestration/crewai_adapter.py
  - src/loan_agents/orchestration/langgraph_adapter.py
  - tests/foundation/test_orchestrator_output_contract.py
  - tests/runtime/test_failure_mission_report.py
autonomous: true
requirements: [RELI-04, QUAL-01]
must_haves:
  truths:
    - "Known unhappy paths return deterministic failure metadata"
    - "Adapter-level failures preserve shared response contract across both modes"
    - "Runtime wrapper unhappy-path errors keep stable mission-report fields"
  artifacts:
    - path: "src/loan_agents/orchestration/normalized.py"
      provides: "Shared deterministic adapter failure contract including failure metadata"
    - path: "tests/foundation/test_orchestrator_output_contract.py"
      provides: "Unit contract assertions for adapter success and failure payload shape"
    - path: "tests/runtime/test_failure_mission_report.py"
      provides: "Unit assertions for deterministic runtime wrapper failure behavior"
  key_links:
    - from: "src/loan_agents/orchestration/crewai_adapter.py"
      to: "src/loan_agents/orchestration/normalized.py"
      via: "build_adapter_failure helper"
      pattern: "build_adapter_failure"
    - from: "src/loan_agents/orchestration/langgraph_adapter.py"
      to: "src/loan_agents/orchestration/normalized.py"
      via: "build_adapter_failure helper"
      pattern: "build_adapter_failure"
    - from: "tests/foundation/test_orchestrator_output_contract.py"
      to: "src/loan_agents/orchestration/normalized.py"
      via: "deterministic error-field assertions"
      pattern: "failure_category|retry_count|stage"
---

<objective>
Harden unit-level deterministic contracts for adapter and runtime unhappy paths.

Purpose: Close RELI-04/QUAL-01 coverage gaps by making failure metadata deterministic and test-enforced.
Output: Updated normalization helper plus deterministic unit tests for adapter and runtime failure behavior.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-deterministic-validation/04-RESEARCH.md
@.planning/phases/04-deterministic-validation/04-VALIDATION.md
@src/loan_agents/orchestration/normalized.py
@src/loan_agents/runtime/service.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Enrich adapter failure normalization with deterministic mission-report fields</name>
  <files>src/loan_agents/orchestration/normalized.py, src/loan_agents/orchestration/crewai_adapter.py, src/loan_agents/orchestration/langgraph_adapter.py, tests/foundation/test_orchestrator_output_contract.py</files>
  <read_first>src/loan_agents/orchestration/normalized.py, src/loan_agents/orchestration/crewai_adapter.py, src/loan_agents/orchestration/langgraph_adapter.py, tests/foundation/test_orchestrator_output_contract.py</read_first>
  <acceptance_criteria>
    - src/loan_agents/orchestration/normalized.py build_adapter_failure includes error fields failure_category, retry_count, and stage.
    - tests/foundation/test_orchestrator_output_contract.py asserts adapter failure includes code DOCUMENT_ERROR and failure_category invalid_document for both modes.
    - Existing success contract assertions remain unchanged for top-level keys status, decision, mode, stages, error.
  </acceptance_criteria>
  <action>Update build_adapter_failure so it returns a deterministic error payload with exact fields: code, message, failure_category="invalid_document", retry_count=0, stage="document". Keep top-level PipelineResult shape unchanged. Ensure both adapters continue calling build_adapter_failure without mode-specific divergence.</action>
  <verify>
    <automated>python -m pytest tests/foundation/test_orchestrator_output_contract.py -q</automated>
  </verify>
  <done>Adapter failure payload is deterministic and unit-tested for both orchestration modes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Strengthen runtime unhappy-path unit checks for deterministic outcomes</name>
  <files>tests/runtime/test_failure_mission_report.py, tests/foundation/test_dual_mode_samples.py</files>
  <read_first>tests/runtime/test_failure_mission_report.py, tests/foundation/test_dual_mode_samples.py, src/loan_agents/runtime/service.py</read_first>
  <acceptance_criteria>
    - tests/runtime/test_failure_mission_report.py adds assertions for stable error code and stage on timeout, rate_limit, and provider failures.
    - tests/foundation/test_dual_mode_samples.py asserts unknown-document failures include deterministic failure metadata from adapter normalization.
    - All tests use explicit fixed correlation IDs and avoid random/time-based expectations.
  </acceptance_criteria>
  <action>Add deterministic assertions that runtime unhappy paths always return stable mission-report fields per scenario: timeout -> code TIMEOUT + failure_category timeout + stage dispatch; rate limit -> code RATE_LIMIT + failure_category rate_limit + retry_count 0; provider -> code PIPELINE_ERROR + failure_category provider. Extend unknown-document parity test to assert invalid_document metadata fields are present and equal across modes (except mode value).</action>
  <verify>
    <automated>python -m pytest tests/runtime/test_failure_mission_report.py tests/foundation/test_dual_mode_samples.py -q</automated>
  </verify>
  <done>Known unhappy paths have deterministic unit-test coverage with clear failure diagnostics.</done>
</task>

</tasks>

<verification>
- python -m pytest tests/foundation/test_orchestrator_output_contract.py tests/runtime/test_failure_mission_report.py tests/foundation/test_dual_mode_samples.py -q
</verification>

<success_criteria>
- Adapter and runtime unhappy paths expose deterministic failure metadata.
- Unit tests explicitly enforce deterministic behavior for RELI-04 and QUAL-01.
</success_criteria>

<output>
After completion, create `.planning/phases/04-deterministic-validation/04-01-SUMMARY.md`
</output>
