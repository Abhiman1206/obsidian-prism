---
phase: 04-deterministic-validation
plan: 02
type: execute
wave: 2
depends_on: [04-01]
files_modified:
  - tests/foundation/test_dual_mode_samples.py
  - tests/runtime/test_runtime_observability_integration.py
  - tests/runtime/test_runtime_api_endpoints.py
  - pytest.ini
autonomous: true
requirements: [QUAL-02, RELI-04]
must_haves:
  truths:
    - "CrewAI and LangGraph produce parity-aligned outcomes for shared scenarios"
    - "Known unhappy paths remain deterministic across repeated runs"
    - "Phase test suite runs unattended with scenario-specific diagnostics"
  artifacts:
    - path: "tests/foundation/test_dual_mode_samples.py"
      provides: "Cross-mode deterministic parity matrix for happy and unhappy scenarios"
    - path: "tests/runtime/test_runtime_observability_integration.py"
      provides: "Integration checks for parity-safe observability and failure consistency"
    - path: "pytest.ini"
      provides: "Phase-focused unattended deterministic test marker/selection support"
  key_links:
    - from: "tests/foundation/test_dual_mode_samples.py"
      to: "src/loan_agents/runtime/service.py"
      via: "run_pipeline matrix execution"
      pattern: "run_pipeline"
    - from: "tests/runtime/test_runtime_observability_integration.py"
      to: "src/loan_agents/runtime/service.py"
      via: "failure and metrics parity integration"
      pattern: "failure_category|metrics"
    - from: "tests/runtime/test_runtime_api_endpoints.py"
      to: "src/loan_agents/runtime/api.py"
      via: "unattended endpoint contract diagnostics"
      pattern: "health|readiness|run"
---

<objective>
Build deterministic integration parity validation and unattended diagnostics for phase completion.

Purpose: Deliver QUAL-02 and finish RELI-04 proof with repeatable, CI-friendly parity checks.
Output: Expanded integration parity matrix, deterministic repeat-run checks, and phase-focused unattended test selection.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-deterministic-validation/04-RESEARCH.md
@.planning/phases/04-deterministic-validation/04-VALIDATION.md
@.planning/phases/04-deterministic-validation/04-01-SUMMARY.md
@src/loan_agents/runtime/service.py
@src/loan_agents/runtime/api.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add integration parity matrix for known happy and unhappy scenarios</name>
  <files>tests/foundation/test_dual_mode_samples.py, tests/runtime/test_runtime_observability_integration.py</files>
  <read_first>tests/foundation/test_dual_mode_samples.py, tests/runtime/test_runtime_observability_integration.py, src/loan_agents/runtime/service.py</read_first>
  <acceptance_criteria>
    - tests/foundation/test_dual_mode_samples.py includes a table-driven scenario matrix covering document_valid_123, document_risky_789, and document_unknown_999.
    - tests/runtime/test_runtime_observability_integration.py includes provider-failure parity checks for both modes with deterministic failure_category provider.
    - Matrix assertions compare sanitized payloads to ignore mode and correlation_id while enforcing equality on status, decision, error.code, error.failure_category, and stage status.
  </acceptance_criteria>
  <action>Refactor parity tests into table-driven matrix cases with explicit scenario labels. Add helper that sanitizes known variance fields (`mode`, `correlation_id`, dynamic metrics values) before equality checks. Add provider failure integration case by monkeypatching each adapter path and asserting deterministic failure parity metadata across modes and repeated invocations.</action>
  <verify>
    <automated>python -m pytest tests/foundation/test_dual_mode_samples.py tests/runtime/test_runtime_observability_integration.py -q</automated>
  </verify>
  <done>Integration parity is enforced for documented happy and unhappy scenarios in both modes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add unattended diagnostics entrypoint for deterministic-validation suite</name>
  <files>pytest.ini, tests/runtime/test_runtime_api_endpoints.py, tests/runtime/test_failure_mission_report.py</files>
  <read_first>pytest.ini, tests/runtime/test_runtime_api_endpoints.py, tests/runtime/test_failure_mission_report.py, .planning/ROADMAP.md</read_first>
  <acceptance_criteria>
    - pytest.ini defines a marker or testpath selector named deterministic_validation.
    - tests/runtime/test_runtime_api_endpoints.py and tests/runtime/test_failure_mission_report.py include marker usage or naming convention supporting deterministic_validation selection.
    - Command `python -m pytest -q -m deterministic_validation` runs unattended and exits 0 when phase implementation is complete.
  </acceptance_criteria>
  <action>Configure pytest to support a deterministic-validation suite entrypoint using marker `deterministic_validation`. Tag the parity and failure-diagnostic tests needed for phase 4 completion. Ensure assertion messages include scenario and mode identifiers so failures are clear in unattended CI output.</action>
  <verify>
    <automated>python -m pytest -q -m deterministic_validation</automated>
  </verify>
  <done>Deterministic-validation checks can be executed unattended with clear failure diagnostics.</done>
</task>

</tasks>

<verification>
- python -m pytest tests/foundation/test_dual_mode_samples.py tests/runtime/test_runtime_observability_integration.py tests/runtime/test_failure_mission_report.py -q
- python -m pytest -q -m deterministic_validation
</verification>

<success_criteria>
- CrewAI and LangGraph parity is validated across known scenarios and unhappy paths.
- Deterministic-validation suite executes unattended with clear diagnostics for failures.
</success_criteria>

<output>
After completion, create `.planning/phases/04-deterministic-validation/04-02-SUMMARY.md`
</output>
