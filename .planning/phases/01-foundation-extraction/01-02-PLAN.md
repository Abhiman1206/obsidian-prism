---
phase: 01-foundation-extraction
plan: 02
type: execute
wave: 2
depends_on: [01-01]
files_modified:
  - src/loan_agents/runtime/errors.py
  - src/loan_agents/runtime/service.py
  - src/loan_agents/orchestration/crewai_adapter.py
  - src/loan_agents/orchestration/langgraph_adapter.py
  - src/loan_agents/orchestration/__init__.py
  - tests/foundation/test_service_entrypoint.py
  - tests/foundation/test_mode_contract.py
  - README.md
autonomous: true
requirements: [ARCH-01]
must_haves:
  truths:
    - "A callable service interface executes with explicit mode selection"
    - "Failure responses are deterministic and structured"
    - "Framework-specific logic is isolated behind thin adapters"
  artifacts:
    - path: "src/loan_agents/runtime/service.py"
      provides: "Callable run_pipeline entrypoint"
    - path: "src/loan_agents/orchestration/crewai_adapter.py"
      provides: "CrewAI adapter wrapper"
    - path: "src/loan_agents/orchestration/langgraph_adapter.py"
      provides: "LangGraph adapter wrapper"
  key_links:
    - from: "src/loan_agents/runtime/service.py"
      to: "src/loan_agents/orchestration/*.py"
      via: "mode dispatch"
      pattern: "mode"
    - from: "tests/foundation/test_mode_contract.py"
      to: "src/loan_agents/runtime/service.py"
      via: "explicit mode + structured failure assertions"
      pattern: "run_pipeline"
---

<objective>
Deliver the callable extraction baseline entrypoint with explicit mode contract and structured failure semantics, using thin framework adapters.

Purpose: Complete Phase 1 success criterion requiring module-level invocation of notebook scenarios.
Output: `run_pipeline` callable, adapter modules, and entrypoint-level tests.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation-extraction/01-CONTEXT.md
@.planning/phases/01-foundation-extraction/01-RESEARCH.md
@.planning/phases/01-foundation-extraction/01-VALIDATION.md
@.planning/phases/01-foundation-extraction/01-01-PLAN.md
@Chapter_15_Agents.ipynb
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build callable service contract with explicit mode dispatch</name>
  <files>src/loan_agents/runtime/errors.py, src/loan_agents/runtime/service.py, tests/foundation/test_service_entrypoint.py</files>
  <read_first>src/loan_agents/domain/contracts.py, src/loan_agents/runtime/settings.py, .planning/phases/01-foundation-extraction/01-CONTEXT.md</read_first>
  <behavior>
    - Test 1: `run_pipeline` accepts structured input and explicit mode.
    - Test 2: Invalid config/mode/provider failures return normalized error envelope.
  </behavior>
  <action>Create `src/loan_agents/runtime/errors.py` with error helpers `build_failure(status, mode, code, message)` returning dictionary keys `status`, `decision`, `mode`, `stages`, and `error`. Create `src/loan_agents/runtime/service.py` with function `run_pipeline(input_payload: dict, mode: str) -> dict` that validates `mode` against `crewai` and `langgraph`, dispatches to adapter callables, and wraps all failures with `build_failure` (no exception leakage, no auto-fallback mode switching).</action>
  <acceptance_criteria>
    - `src/loan_agents/runtime/service.py` contains `def run_pipeline(`.
    - `run_pipeline` rejects unknown modes with structured failure output.
    - Output dictionary always includes keys `status`, `decision`, `mode`, `stages`, `error`.
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest tests/foundation/test_service_entrypoint.py -q</automated>
  </verify>
  <done>Callable entrypoint exists and enforces deterministic mode/error behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add thin orchestration adapters for CrewAI and LangGraph</name>
  <files>src/loan_agents/orchestration/crewai_adapter.py, src/loan_agents/orchestration/langgraph_adapter.py, src/loan_agents/orchestration/__init__.py, tests/foundation/test_mode_contract.py</files>
  <read_first>Chapter_15_Agents.ipynb, src/loan_agents/runtime/service.py, .planning/phases/01-foundation-extraction/01-CONTEXT.md</read_first>
  <behavior>
    - Test 1: Service dispatch invokes adapter matching explicit mode.
    - Test 2: Adapter failures return structured failures with selected mode preserved.
  </behavior>
  <action>Create adapter functions `run_crewai_pipeline(input_payload: dict) -> dict` and `run_langgraph_pipeline(input_payload: dict) -> dict` in respective adapter modules. Keep adapters thin: map input payload, call placeholder/extracted implementation hooks, return normalized dictionary, and avoid domain logic duplication. Export adapters via `src/loan_agents/orchestration/__init__.py` for service imports.</action>
  <acceptance_criteria>
    - Adapter modules each expose exactly one run function with expected names.
    - Service file imports adapters from orchestration package.
    - Adapter code does not define independent schema classes.
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest tests/foundation/test_mode_contract.py -q</automated>
  </verify>
  <done>Mode-specific orchestration is isolated to thin adapters and wired to service.</done>
</task>

<task type="auto">
  <name>Task 3: Document extraction entrypoint and execute foundation smoke verification</name>
  <files>README.md, tests/foundation/test_service_entrypoint.py, tests/foundation/test_mode_contract.py</files>
  <read_first>.planning/ROADMAP.md, .planning/phases/01-foundation-extraction/01-VALIDATION.md, src/loan_agents/runtime/service.py</read_first>
  <action>Add a `## Foundation Extraction Runtime` section to `README.md` documenting `run_pipeline` signature, accepted `mode` values (`crewai`, `langgraph`), required env var `LLM_API_KEY`, and guaranteed structured failure behavior. Ensure smoke assertions cover one valid mock document invocation path and one failure path per mode.</action>
  <acceptance_criteria>
    - `README.md` contains heading `## Foundation Extraction Runtime`.
    - `README.md` explicitly documents `LLM_API_KEY` requirement.
    - Full foundation test command passes.
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest tests/foundation -q</automated>
  </verify>
  <done>Entrypoint behavior is documented and verified by repeatable smoke tests.</done>
</task>

</tasks>

<verification>
- `python -m pytest tests/foundation -q` passes after adapter wiring.
- `README.md` documents callable interface and mode/error contract.
- No automatic fallback between orchestration modes exists in service logic.
</verification>

<success_criteria>
- Phase 1 module extraction can be invoked through a deterministic callable.
- Structured failure contract is stable and mode-aware.
- Foundation runtime contract is clear enough for Phase 2 expansion.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-extraction/01-02-SUMMARY.md`
</output>
