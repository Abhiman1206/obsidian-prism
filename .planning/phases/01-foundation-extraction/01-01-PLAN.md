---
phase: 01-foundation-extraction
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/loan_agents/domain/contracts.py
  - src/loan_agents/runtime/settings.py
  - src/loan_agents/runtime/redaction.py
  - src/loan_agents/document/mock_data.py
  - tests/foundation/test_contracts.py
  - tests/foundation/test_settings.py
  - pytest.ini
autonomous: true
requirements: [ARCH-01, SECU-01, SECU-02]
must_haves:
  truths:
    - "Core notebook data contracts exist as importable Python models"
    - "Runtime configuration fails fast when required settings are missing"
    - "No runtime path depends on interactive secret input"
  artifacts:
    - path: "src/loan_agents/domain/contracts.py"
      provides: "Shared request/response contract models"
    - path: "src/loan_agents/runtime/settings.py"
      provides: "Typed non-interactive runtime settings"
    - path: "src/loan_agents/document/mock_data.py"
      provides: "Single canonical mock document provider"
  key_links:
    - from: "src/loan_agents/runtime/settings.py"
      to: "LLM_API_KEY"
      via: "required field validation"
      pattern: "LLM_API_KEY"
    - from: "tests/foundation/test_settings.py"
      to: "src/loan_agents/runtime/settings.py"
      via: "missing-key failure assertions"
      pattern: "raises"
---

<objective>
Establish the Phase 1 extraction foundation with feature-first package structure, shared contracts, strict settings validation, and Wave 0 tests.

Purpose: Implement the minimum production-safe baseline for ARCH-01, SECU-01, and SECU-02.
Output: Importable modules for contracts/settings/mock-data and baseline tests proving config behavior.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation-extraction/01-CONTEXT.md
@.planning/phases/01-foundation-extraction/01-RESEARCH.md
@Chapter_15_Agents.ipynb
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create contracts and feature-first package skeleton</name>
  <files>src/loan_agents/domain/contracts.py, src/loan_agents/document/__init__.py, src/loan_agents/credit/__init__.py, src/loan_agents/risk/__init__.py, src/loan_agents/compliance/__init__.py</files>
  <read_first>Chapter_15_Agents.ipynb, .planning/phases/01-foundation-extraction/01-CONTEXT.md</read_first>
  <behavior>
    - Test 1: Required pipeline input fields map to typed request schema.
    - Test 2: Normalized output schema always includes status, decision, mode, stages, error.
  </behavior>
  <action>Create `src/loan_agents/domain/contracts.py` with concrete models named `PipelineInput`, `StageResult`, and `PipelineResult`; include fields `applicant_id: str`, `document_id: str`, `mode: str` in input model, and `status: str`, `decision: str`, `mode: str`, `stages: dict`, `error: dict | None` in result model. Create `__init__.py` files for `document`, `credit`, `risk`, and `compliance` feature folders to establish feature-first package structure per D-01 and D-02.</action>
  <acceptance_criteria>
    - `src/loan_agents/domain/contracts.py` contains `class PipelineInput`.
    - `src/loan_agents/domain/contracts.py` contains `class PipelineResult`.
    - Feature directories exist with `__init__.py` files for document/credit/risk/compliance.
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest tests/foundation/test_contracts.py -q</automated>
  </verify>
  <done>Typed contract module exists and package skeleton is importable.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement non-interactive settings and redaction helpers</name>
  <files>src/loan_agents/runtime/settings.py, src/loan_agents/runtime/redaction.py, src/loan_agents/runtime/__init__.py, tests/foundation/test_settings.py</files>
  <read_first>Chapter_15_Agents.ipynb, .planning/phases/01-foundation-extraction/01-CONTEXT.md, .planning/REQUIREMENTS.md</read_first>
  <behavior>
    - Test 1: Missing `LLM_API_KEY` causes startup validation error.
    - Test 2: Secret values are redacted from formatted error/log text.
  </behavior>
  <action>Create `src/loan_agents/runtime/settings.py` using a typed settings class with required key `LLM_API_KEY` and optional keys `LLM_MODEL` defaulting to `gemini-3-flash-preview` and `LOG_LEVEL` defaulting to `INFO`. Implement strict validation that raises on missing/empty `LLM_API_KEY`. Create `src/loan_agents/runtime/redaction.py` exposing `redact_secret(value: str) -> str` that returns fully masked output (`***REDACTED***`). Ensure no interactive `getpass` is used in runtime modules.</action>
  <acceptance_criteria>
    - `src/loan_agents/runtime/settings.py` contains `LLM_API_KEY` as required field.
    - `src/loan_agents/runtime/settings.py` has no `getpass` reference.
    - `src/loan_agents/runtime/redaction.py` returns `***REDACTED***` for non-empty input.
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest tests/foundation/test_settings.py -q</automated>
  </verify>
  <done>Runtime settings are environment-driven, fail-fast, and redact secrets consistently.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Unify duplicated mock document provider and bootstrap Wave 0 tests</name>
  <files>src/loan_agents/document/mock_data.py, tests/foundation/test_contracts.py, tests/foundation/test_settings.py, pytest.ini</files>
  <read_first>Chapter_15_Agents.ipynb, .planning/phases/01-foundation-extraction/01-VALIDATION.md</read_first>
  <behavior>
    - Test 1: `document_valid_123`, `document_risky_789`, and `document_invalid_456` resolve to deterministic payloads.
    - Test 2: Unknown document ID returns explicit error payload.
  </behavior>
  <action>Create `src/loan_agents/document/mock_data.py` with one canonical `get_document_content(document_id: str) -> str` containing all three known scenario payloads plus unknown-id error payload. Add `tests/foundation/test_contracts.py` and `tests/foundation/test_settings.py` initial assertions matching validation strategy, and create `pytest.ini` with `testpaths = tests` and `python_files = test_*.py`.</action>
  <acceptance_criteria>
    - `src/loan_agents/document/mock_data.py` contains all scenario IDs: `document_valid_123`, `document_risky_789`, `document_invalid_456`.
    - `pytest.ini` exists and contains `testpaths = tests`.
    - Both test files exist and run via pytest command.
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest tests/foundation -q</automated>
  </verify>
  <done>Single canonical mock-data helper and baseline test harness are in place.</done>
</task>

</tasks>

<verification>
- `python -m pytest tests/foundation -q` passes.
- `src/loan_agents/runtime/settings.py` contains no interactive secret prompt usage.
- `src/loan_agents/document/mock_data.py` is the only canonical mock document provider module.
</verification>

<success_criteria>
- ARCH-01 addressed by package extraction baseline and shared contracts.
- SECU-01 addressed by environment-only settings loading.
- SECU-02 addressed by strict startup validation checks.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-extraction/01-01-SUMMARY.md`
</output>
