VITE_RUNTIME_API_URL=http://localhost:8000
# Optional: set true to force mock mode even in deployed environments.
VITE_USE_MOCK=false
# Optional: request timeout in milliseconds.
VITE_RUNTIME_TIMEOUT_MS=10000
# Optional: expected frontend origin for backend CORS docs and diagnostics.
VITE_FRONTEND_ORIGIN=http://localhost:5173